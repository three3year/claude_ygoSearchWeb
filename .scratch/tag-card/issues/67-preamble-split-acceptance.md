# 67 — 前言拆句驗收收尾與資訊清單

**What to build:** 拆分穩定後的人工防線與對帳:句序調換的沉默錯配抽查、編號句換行差異的一次性資訊清單、最終數字對帳。照票09 的驗收慣例收尾,完工後票64(父票)的驗收清單全數承接完畢。

**Blocked by:** 66 — 引用跨行合併

**Status:** done

- [x] 抽查:從「中文行數少於日文」的 262 段中抽 10 段,人工核對中文分組與日文行的配對正確(重點:句序調換且句數恰好相等的沉默錯配,ADR-0009 已知限制);發現錯配即記錄並評估是否需要拆句表人工兜底
- [x] 編號句(①②…)內部換行差異的一次性資訊清單:報告留一節列出 id / index / 兩側行數,**不動資料**(● 拆分走 `●` 字元不讀換行,已確認無拆錯風險)
- [x] 最終對帳:拆分段數(預期 ~1,500 段拆出 ~3,000+ 列)、退回清單餘量與逐筆原因、role 分布(使用次數限制列數上升幅度)、`substring_violations` 0、`non_effect_outside_preamble` 未上升——數字寫進本票 Comments
- [x] 前端索引最終重建,抽 2~3 張卡在網站上目視確認前言句細分顯示正常(含 70488851)
- [x] 結案票64:逐項核對其檢核清單確實被 65/66/67 的成果覆蓋後**逐項打勾**,Comments 註記各項由哪張子票承接,Status 由 superseded 改為 done

## Comments

2026-08-14 完工。

**抽查(10/10 正確)**:以種子 20260814 從拆過且中文行數少於日文的段落(重建計數 234 段;spec 的 262 是含退回與整段被括住者的全母體)抽出 10 段:5141117 / 29439831 / 31086840 / 46898368 / 48815792 / 60004971 / 73490417 / 73819701 / 82301904 / 93039339。逐段核對中文分組與日文行的配對——含 82301904(三段)與 93039339(三段)這種多段形態——**無任何句序錯配**,不需動用拆句表兜底。

**編號句換行差異資訊清單**:196 句,全部是中文行數多於日文(繁中來源在 ● 前補換行等排版差異;其中 84 句含 ●,● 拆分走 `●` 字元不讀換行,無拆錯風險)。spec 預估的 130 是開票時以不同口徑掃的;本清單以拆句骨架的編號句(● 拆分前)兩側行數差計,已印進建置報告的固定資訊節,資料未動(本次重跑 `data/tag_cards.json` 零 diff)。

**最終對帳**(對照票64 前基準):

| 項目 | 基準 | 完工 |
|---|---|---|
| 前言列 | 8,038 | 9,666 |
| 拆分 | — | **1,483 段 → 3,111 列**(預期 ~1,500 → ~3,000+ ✓) |
| 退回清單 | — | **3 段**,全部單位數不合 |
| 整段被引用括住維持一句 | — | 24 段(官方視為一體,不拆是正確結果) |
| role=使用次數限制 | 4,484 | **5,499(+1,015)** |
| role=召喚條件 | 657 | 964 |
| role=素材指定 | 2,358 | 2,358(不變) |
| role=null | 539 | 845 |
| `substring_violations` | 0 | 0 |
| `non_effect_outside_preamble` | 84 | 84(未上升) |
| 既有判定孤兒 | 0 | 0 |

退回 3 段逐筆原因(皆為繁中譯文句數與日文行內句號數真的對不上,列為日後拆句表人工兜底候選):

- 35606858(萬魔殿-惡魔的巢窟-):日文第二行 3 句(払う。/破壊する。/発動できない。),繁中把前兩句併成一句(「支付500生命值,或不支付讓此卡破壞。」)
- 40080312(E.HERO 神之新宇俠):繁中把「(不需要「融合」)」寫在句號之外自成一單位,日文寫在句號之內
- 50604072(/爆裂模式):繁中「此卡不能通常召喚。只能…特殊召喚。」兩句對日文一句(「通常召喚できず、…でのみ特殊召喚できる。」)

**前端索引與目視確認**:索引已最終重建、全部檢查通過。以索引實際載入的 `web/data.js` 逐欄核對 70488851(0-1 召喚條件 cond / 0-2 使用次數限制 limit)、2133971(素材 mat / 召喚方法 / 使用次數限制 limit 三段)、82301904(cond / cond / null 三段),前言句細分與 role 標記顯示資料正常(本機無瀏覽器自動化工具,以網站唯一讀的資料檔核對代替截圖)。
