# 22 — 判定票 05/33 — 要拆+判 split-06

**What to build:** 要拆+判系列第 6 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、21 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 6` → `.scratch/tag-card/batches/split-06.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-06.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-06.json --batch .scratch/tag-card/batches/split-06.json --ticket 票22`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票22 回報(2026-08-10)

- **拆句**:200 團全數寫入拆句表,拆成 **310 段**(效果句 260 + 效果外文本 50;單段 127 團 / 多段 73 團)。三道關卡 dry-run 一次過 0 問題,正式寫入未用 `--force`。
- **兩側語序不一致**:1 張給了 `ja_order`(旗鼓堂々 25067275)——繁中把「1回合只能發動1張」的卡名發動限制提到最前面、日文在句尾,與票19/21 的樣板一致,`ja_order` 為 `[1, 0]`、限制段照判準13/5 記 `index "0"`。
- **判定**:判了類型 **274 條**,其中寫入 llm **105 條**(1 條與[[影子預測]]一致升 `llm_then_rule`:機甲忍者アース 22812068,無種類效果)、與[[官方明示]]一致而由官方接手 **169 條**;[[必發/選發]]由判定寫入 **72 條**(其餘由官方明示階梯與 `_optional_by_rule()` 接手)。**留空 0 條**。
- **準確率(免費量測)**:因拆句在本票段落上新取得[[官方明示]] **161 條淨增**(8 → 169;原 8 列是管線先前在未拆整團上 `●` 對位的 official 列,拆句後由新結構重新對位接手,值全部相同——報告的 `既有判定對不到任何一行` 8 筆:22377815、23118924×2、23454876、23776077×3、26254876),**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆)。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空。批次帶影子預測的 11 條中,判成[[魔陷卡效果]]的 3 條(魔力誘爆 22869904、念導力 23323812、王宮の牢獄 26586849)不在規則層管轄、照 ADR-0004 跳過比對,其餘 8 條(22812068、24317029、24996659、25034083、25460258、26722601、27132350、27189308)全部判怪獸六類且與預測一致。
- **歸納進規則層**:新登記 **R13**(自己得到型純領起句→效果外文本):單一句子、主語このカード自己、「以下の効果を得る」收尾、無發動子句——全表官方給過答案的 **41/41** 全是「効果の扱いではありません」,規則層量測覆蓋 57、官方 34/34 一致 100%。R13 進場後與 R7 在 4 行單句領起句上重疊且結論不同(官方那族 3 筆全判效果外文本,正是票21 時 R7 對官方 5 筆不一致中的 3 筆),照 `CHANGE_OVERLAP` 協定收緊 R7(排除「以下の効果を得る」)——R7 對官方的不一致由 3 筆歸零,規則層整體獨立驗證 **4339/4341 = 100.0%**,13 條全部 PASS 生效。本卡 23776077(陽炎獣 バジリコック)的領起句即照這一族判效果外文本(補足未明寫)。
- **歸納進規範**:§5.5 分界四補「自己得到型純領起句 41/41」一條(效果外文本的家族判法與單句限定的理由)。**無新拆句判準**——本票拆點全部落在既有判準 5/9/12/13/16/17/19/20 與官方引用邊界上:選択型 `●` 列舉(22339232、23212990、24673894、24857466)照判準9 不拆;賦予型領起句+行內 `●` 引用(22377815、23118924、23454876、23776077、26254876)照判準9/19 自行拆;有 `【…●について】` 標頭的(26931058)照判準19 只寫一段交給管線;卡名限制句夾在效果中間的(26285788)照判準20 不抽。
- **測試**:tag_card 247+47、card_list 16+13、faq_info 14+29,全部通過。
