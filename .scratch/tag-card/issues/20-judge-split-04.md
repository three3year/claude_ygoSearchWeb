# 20 — 判定票 03/33 — 要拆+判 split-04

**What to build:** 要拆+判系列第 4 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、19 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 4` → `.scratch/tag-card/batches/split-04.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-04.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-04.json --batch .scratch/tag-card/batches/split-04.json --ticket 票20`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票20 回報(2026-08-10)

- **拆句**:200 團全數寫入拆句表,拆成 **334 段**(效果句 281 + 效果外文本 53;單段 114 團 / 多段 86 團)。三道關卡 dry-run 一次過(0 個問題),正式寫入未用 `--force`。
- **兩側語序不一致**:3 張給了 `ja_order`(双龍降臨 13166648、アンドロ・スフィンクス 15013468、神の威光 16308000)——都是繁中把「1回合1張」「不能從墓地特殊召喚」「發動後第N回合自毀」這種框架句提到最前面的形態,與票19 的樣板一致。
- **判定**:判了類型 **278 條**,其中寫入 llm **83 條**、與[[官方明示]]一致而由官方接手 **213 條**;[[必發/選發]]判了 **91 條**寫入。
- **留空 3 條**(附理由在結果檔 note):15415552 與 16237004 整團留空(來源資料**缺日文卡文與補足**,`text_ja` 為空字串,無從拆句與判定,之後每票會再出現直到資料補齊);16308000 idx3(`発動後２回目の…このカードを墓地へ送る`,同型句官方三種都判過、本卡補足沒講連鎖,見 §5.9)。
- **準確率(免費量測)**:因拆句新取得[[官方明示]] **213 條**(拆句表整體 535 → 748),**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆)。
- **`rule_llm_conflict`**:本票新增 **0 筆**。批次帶[[影子預測]]的 6 條(13220032、13316346、13532663、15187079、16475472、16768387)全部與判定一致;拆句後規則層對本票新段落補出的 11 條預測也全部與判定一致(3 條升 `llm_then_rule`)。衝突清單維持既有 1 筆(ヴァイロン・セグメント 1644289,§5.2 記載的預期衝突),收斂狀態因此仍為「未收斂」,非本票造成。
- **獨立驗證**:必發/選發 848/849(唯一不一致仍是 §6 記載的救世星龍 7841112);效果類型規則層 4288/4293,12 條規則全部 PASS 生效,無新規則進場(発動後Ｎ回目族官方類型明示 0 條、覆蓋不足)。
- **§5.8 量測(三票同法)**:判定填了必發/選發 134 條 / 規則有預測 67 條 / **不一致 7 條,全部是魔法・陷阱卡的卡本身發動**;`〜する事で` 族如預告歸零。已補記進 §5.8。
- **歸納進規範**:新增 **§5.9**「発動後Ｎ回目の〜」的延後處理——前言被動態壽命句 10/10 官方判效果外文本;主動態帶額外處理的看補足連鎖敘述(作られる→誘發1速 3 條、作られません→無種類 3 條);補足沒講的留空。同型的爆弾ウニ 52140003(票51 判誘發1速,無補足依據)列**待改判票統一**。無新拆句判準(本票拆點全部落在既有判準 5/9/11/12/13/14/16/19 與官方引用邊界上,不需掃描)。
- **舊 ● 對位淘汰 2 列**:13030280 與 13391185 先前由管線在未拆整團上拆出的 `1-●1` official 列,在拆句後對不到新結構(報告的 `既有判定對不到任何一行` 2 筆),類型由官方在新段落上重新對位接手,值相同(誘發1速 / 啟動),無資料損失。
