# 55 — 缺日文卡文的整團:退化拆句清單與補齊後的重拆重判

**What to build:** 來源資料缺日文卡文與[[補足情報]]的卡(`text_ja` 為空字串),判定票只能整團留空。目前的記錄形式是**單段、`text_ja` 空字串的退化拆句紀錄**(帶 `text_hash` 入拆句表),偏離 `merge_judgments.py` 契約文件化的「空 `clauses` = 拆不出來」路徑;機制上日文補齊時 `split_hash` 變動 → `split_stale` 會自動把整團退回待拆佇列,但補齊之前它們不會再出現在任何批次裡。本票收攏全部這類卡,等**判定票系列(票22–50)全部完成後**依次處理。

**Blocked by:** 判定票系列全部完成(要拆+判 票22–36、只判類型 票37–50)

**Status:** ready-for-agent

## 待處理清單

建置報告「無日文卡文」全表 17 張(2026-08-10 時點):

- **已寫成退化拆句(4 張)**:15415552、16237004(票20);17350692、17782288(票21)
- **尚未判到,輪到時會照同型處理**:28904860、42410161、43632709、53754391、54171327、54577949、55393975、68811206、80565021、80843006、81797573、81965543、89027418

## 工作契約

- [ ] 補齊來源資料的日文卡文與補足情報(走 faq-info 的補抓流程,參照 `.scratch/faq-info/issues/06-refill-missing-cards.md` 的做法)
- [ ] 補齊後跑一次建置,確認 `split_stale` 把已寫退化拆句的卡全部退回待拆(`pending_split`),報告的「無日文卡文」歸零
- [ ] 用 `make_batches.py --only <密碼,...> --name refill-ja` 產補判批次,重拆重判這批卡
- [ ] 順帶決定記錄形式的定案:退化拆句(現行,靠 `split_stale` 重浮)或空 `clauses`(契約文件化路徑,每票重現)——擇一寫進 `merge_judgments.py` 的契約說明,4 張既有紀錄若改路徑要一併清掉
- [ ] 只 commit 到本地分支,不 push

## Comments

票39(只判類型 kind-03)實測到退化拆句的**另一個後果**:那 4 張因為已經有拆句紀錄,現在會以「整團一段、`index` 為 `1`」的形式落進**只判類型**的批次(4 張全在 kind-03 裡),而那一段放不下多個效果句的類型 → 判定票只能整段留空(該票 5 條留空裡有 4 條是它們)。也就是說,補齊之前它們**每一票都會再出現一次**,不是「掉出佇列」;本票收尾時一併確認記錄形式要不要改成空 `clauses`。

出自票21 的 /code-review(hygiene 角度):退化拆句讓這些卡掉出待拆佇列,與 note「下一票會再出現」的字面不符;但 `text_hash` 過期機制保證「待資料補齊後重判」仍成立,故降為低嚴重度、集中到本票收尾。使用者裁定:先記錄,等判定票全部完成再依次處理(2026-08-10)。
