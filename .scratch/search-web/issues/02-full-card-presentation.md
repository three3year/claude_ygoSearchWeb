# 02 — 完整卡片呈現

**What to build:** 卡片長成最終的樣子。使用者看到卡圖、完整的卡面資訊、以及一份讀起來
像卡片而不像 JSON 的卡文——[[拆句表]]與 20 多張判定票的成果在這裡第一次對使用者可見。

視覺沿用 oldProject(`F:\AiProject\oldProject`)驗證過的顯示手法,`style.css` 與 `render.js`
從該專案拉過來改。深色配色與 CSS 變數結構照舊。呈現層採**全量重繪**:本站有分頁,單次
渲染量只有一頁;差異更新需要「知道上次渲染了什麼」,那是把耦合換個名字留下來。

[[前端索引]]同步補齊本票需要的欄位。

**Blocked by:** 01 — 值域正典與最小索引

**Status:** ready-for-agent

- [ ] 卡圖走 `cdn.jsdelivr.net/gh/salix5/query-data@master/pics/<密碼>.jpg`,
      `loading="lazy"`,載不到時安靜隱藏(不留破圖)
- [ ] [[同名異圖卡]]的卡圖下方出現 `‹ 1/17 ›` 箭頭,可前後切換;只在 342 張有異圖的卡上
      渲染,其餘 13,865 張不佔版面
- [ ] 異圖切換只換圖,卡片其他資訊不動(異圖是同一張卡)
- [ ] 卡片密碼顯示時補零成 8 位
- [ ] 點卡名另開 `salix5.github.io/query/?code=<密碼>`
- [ ] 中日英卡名副標;`※` 別名(117 張)顯示在副標且**不出現在卡文裡**
- [ ] 怪獸參數區塊:等級/階級/連結值、屬性、種族、刻度、攻/守;`?` 顯示為 `?` 而不是 0;
      連結怪獸不顯示守備
- [ ] 連結標記九宮格
- [ ] 靈擺卡依 `pz` 分區,重建 `【靈擺效果】` / `【怪獸效果】` 標頭與分隔線
- [ ] [[效果外文本]]依 `role` 分三種弱化樣式(素材指定/召喚條件/使用次數限制);
      素材指定行另依召喚法(融合/同調/超量/連結/儀式)上色
- [ ] 689 張純通常怪獸顯示故事文,淡化樣式
- [ ] MD 稀有度與 Genesys 點數顯示
- [ ] 索引補齊 `al` / `ax` / `at` / `r` / `lv` / `lk` / `sc` / `lm` / `atk` / `df` /
      `ot` / `ra` / `gy` / `ro` / `pz`,全部走短碼
- [ ] `※` 別名從卡文抽出進 `ax` 這件事有測試蓋住(容易寫成「顯示時再剝掉」)
