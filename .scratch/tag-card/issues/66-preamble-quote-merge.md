# 66 — 引用跨行合併

**What to build:** 票65 退回的大宗救回來:補足情報 `『…』` 引用橫跨拆點時,不再整段退回,而是**把被引用括住的那幾行合為一段**再拆——官方逐項裁定視為一體的單位不切開。完工後 70488851 拆成 2 句(前兩行合為召喚條件 + 使用次數限制一句),退回清單大幅縮短,清單裡剩下的只有單位數真的對不上的段落。

**Blocked by:** 65 — 純換行前言拆分

**Status:** ready-for-agent

- [ ] 拆點與引用比對:引用經正規化後落在日文前言的哪個行區間,跨行的引用把該區間合為一段;合併後所有引用必須完整落在單一段內,否則仍整段退回(不猜測的底線不變)
- [ ] 中文分組照票65 的演算法,以合併後的段為分組目標
- [ ] 測試(同票65 接縫):① 70488851 形態——三行、引用括前兩行,拆成 2 段且 role 分別為召喚條件/使用次數限制;② 引用型效果外明示落到正確細段(source 升 official);③ 合併後仍有引用被切開的構造 → 整段退回
- [ ] 全表重跑:退回清單筆數顯著下降(票65 基準對比)、「效果外明示不在前言段」(`non_effect_outside_preamble`)筆數不得上升、`substring_violations` 維持 0
- [ ] 無補足情報的卡行為與票65 完全相同(純換行),回歸測試釘住
