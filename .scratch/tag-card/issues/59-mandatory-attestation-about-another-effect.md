# 59 — 必發明示的歸屬:標頭底下講「別的效果」的那一行被照字面吃了

**What to build:** [[必發/選發]]官方明示階梯的一道排除:`【…について】` 標頭底下、句子在解說**被複製/被參照的另一個效果**怎麼運作時,那一行的「必ず発動する効果」不指向本[[效果句]],不得產生必發明示。

**Blocked by:** 38 — 只判類型 kind-02(問題在那一票撞牆)

**Status:** done

## 為什麼

`docs/effect_kind_guide.md` §6 從票19 起就記著這件事,但只記在**判定者**那一側(「判到『這一行在解說別的卡/別的效果怎麼運作』時,不採用」)。抽取器的必發階梯沒有這道排除,照字面把它收成本效果句的必發明示。

救世星龍(7841112)的 `main/3`:

```
【『この効果で無効にしたモンスターに記された効果を、このターンこのカードの効果として１度だけ発動できる』について】
■…そのモンスターのテキストに記載されている効果を、このカードの効果として発動できます。
■これによって発動する効果の、効果の種類（起動効果・誘発効果・誘発即時効果）、発動の条件、…については、元となった効果と同じです。
■無効にしたモンスターのテキストに、"条件を満たした際に必ず発動する効果"が記されている場合でも、
  このカードがその効果を発動するかどうかは任意です。
```

第三行的「必ず発動する効果」講的是**被複製的那個效果**,而且整句的結論恰好相反(「このカードがその効果を発動するかどうかは任意です」)。抽取器仍把 `main/3` 標成有必發明示。

**後果是這一行判不進去**。它自己不發動(句尾的「発動できる」也是被複製效果的),走 §5.5 六條分界都不成立、回 §1 步驟 4 會落在永續效果——但 `tagcard._apply_optional` 對「有必發明示、類型卻不發動」的行會記進 `mandatory_other_kind`,而建置報告把它標成**必須為 0**。於是:

- 填永續效果(或任何不發動的類型)→ 報告的不變量從 0 變成 1;
- 留空 → 不變量保住,但這一行每一票都會再回到批次裡,再撞一次牆。

票38 選了留空(§9),並開這張票。這不是「判不出來」,是抽取器的歸屬有洞。

順帶:同一行也讓必發/選發的獨立驗證固定多一筆不一致(`規則=選發、官方=必發`,票31 起記在 §4 的次元壺旁邊)。修掉這道歸屬,那一筆也會一起消失。

## 工作契約

- [x] **量清楚範圍**(先做,決定判別條件寫不寫得出來):全表補足情報裡,落在 `【…について】` 標頭底下、含必發句式、而句子主語是**別的效果**的行有幾筆?候選判別條件:同一句裡出現 `そのモンスター(の)テキスト` / `記されている` / `元となった効果` / 引號包住的 `"…必ず発動する効果"`(**雙引號**,官方拿它標示被引述的概念而非本卡效果)。逐條列出命中的行與它們現在產生的必發明示,確認沒有誤傷真正的必發明示
- [x] **實作**:`official` 側的必發抽取加這道排除;判別條件寫得出來才做,寫不出來就改成「列進報告的待人工判讀清單」並在此回報
- [x] **測試**:命中行不產生必發明示、正常的 `■…必ず発動する効果です。` 照舊產生、括弧內的變體不受影響
- [x] **回歸**:重跑建置,確認 `mandatory_other_kind` 仍為 0、必發獨立驗證的不一致從 2 筆降為 1 筆(次元壺 73414375 那筆是個案,留著)
- [x] **處理 7841112 `main/3`**:~~補判~~ **改成修拆句**——使用者指出票19 那個拆點本身就是錯的(判準17 的例外要求官方另外給**類型**,那個標頭底下沒有),併回 `main/2` 之後整段由官方定案為啟動效果,不需要判定
- [x] **文件**:`docs/effect_kind_guide.md` §6 那一條從「判定者不採用」補成「抽取器也不採用」;§4 第一層的獨立驗證數字重測
- [x] 只 commit 到本地分支,不 push

## Comments

票38 的 300 張裡只有這一條撞牆,但它是**每一票都會再出現**的一條(留空的行會回到批次)。範圍量出來若只有個位數,做窄一點的排除就夠;若寫不成判別條件,退而求其次是把它列進報告,讓判定者知道那一行的必發明示不可信。

### 票38 回報(2026-08-12)

使用者複查票38 的留空時指出兩件事,兩件都改變了這張票的做法:

1. **7841112 的拆點本身是錯的**。票19 看到官方開了 `【『この効果で無効にした…』について】` 標頭就拆,但 §8 判準17 的例外要求官方**另外給一個類型**,那個標頭底下寫的全是「被複製的那個效果」怎麼運作(種類跟著原效果、發不發動跟著原效果),一個指向本段的類型詞都沒有 → 照判準14 不該拆。**併回 `main/2`** 之後,整段由另一個標頭(`【『１ターンに１度、…無効にできる』効果について】■モンスターゾーンで発動できる起動効果です。`)定案為**啟動效果**、`source: official`,連判定都不用。
2. **QA 那句「効果の種類（起動効果・誘発効果・誘発即時効果）…については、元となった効果と同じです」講的是被複製效果的類型,不是這個動作的類型,無需特別處理**——實測確實如此:那一行的類型詞在圓括弧裡而且一行三種,`_line_kinds` 早就因為「一句出現多於一種類型詞」而跳過它。

但**必發那一行仍然要處理**:製造出必發明示的不是上面那一行,而是 `■無効にしたモンスターのテキストに、”条件を満たした際に必ず発動する効果”が記されている場合でも、このカードがその効果を発動するかどうかは任意です。`。合併拆句之後這道明示會跟著搬到 `main/2`,而 **啟動效果同樣不承載必發/選發** → `mandatory_other_kind` 從 0 變 1,不變量照樣破。所以這張票的實作照原計畫做完。

- **判別條件**:官方拿**全形雙引號** `”…”` 標示「被引述的概念」,與 §7 那條「明示句要出現在括弧之外才算數」同一個道理,只是換了一種標記法。`official._line_mandatory()` 先剝掉 `”…”` 再掃 `必ず発動`。
- **量測**(先做才實作):全表必發明示 **1,518 行**,剝掉引號後失去標記的**只有這 1 行**;含全形雙引號的明示行共 **167 行**,剝掉後**類型**結論改變的 **0 行**——所以只動必發那一側,`_line_kinds` 一個字都不改。
- **測試**:新增 2 條(引號內的必發不算數且 `mandatory_other_kind` 為空、同一行引號外的必發照舊算數),tag_card 311 → **313**。
- **回歸**:`mandatory_other_kind` **0** ✓;必發獨立驗證 880/882 → **880/881 = 99.9%**,整份驗證裡只剩次元壺 73414375 那個個案;效果句 32,594 → **32,593**(合併 −1);官方明示 17,375 條不變;`judgment_orphans` 0。
- **票38 的留空因此歸零**:`kind-02.json` 那一行(`(7841112, main, 3)`)已從結果檔移除——那個 index 不再存在,留著會讓結果檔重跑時對不回效果句(關卡三)。票38 的判定條數因此是 **488 條、留空 0 條**。
