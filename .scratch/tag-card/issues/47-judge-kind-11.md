# 47 — 判定票 30/33 — 只判類型 kind-11

**What to build:** 只判類型系列第 11 批(300 張卡)的[[效果類型]]與[[必發/選發]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、46 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series kind --limit 1 --start 11` → `.scratch/tag-card/batches/kind-11.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。本系列的卡文已經有①②③編號,效果句集合已定,不需要拆句,§8 用不上。
- [x] **逐卡判定**:每個待判效果句的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/kind-11.json`(入版控,它是這一票的稽核軌跡)。判不出來的留空並寫 `note`,合併程序會把它們列進待審
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/kind-11.json --batch .scratch/tag-card/batches/kind-11.json --ticket 票47`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。本系列不動 §8
- [x] **回報**:本票判了幾條、與[[官方明示]]一致而留 `official` 幾條(這是準確率的免費量測)、留空待審幾條,以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 300 張。

## 票47 回報(2026-08-12)

**判定量**:300 張卡 / 301 段 / 491 條效果句(調弦的魔術師 73941492 同時有 main 與 pendulum 兩段待判,所以段落數比卡數多 1)。判定寫入 **486 條**,留空 **5 條**;結果檔給了[[必發/選發]] **90 條**,合併後全部落地且值一致(合併程序報「判定給的必發/選發 76 條」,差額 14 條是那些行上規則層/官方本來就已經有同值;`類型不承載必發/選發(已丟棄): 0 筆`)。

**留空 5 條**(與票40–46 同一批既有管線缺口,不是判不出來):15415552 / 16237004 / 17350692 / 17782288 `main/1`(票55 的無日文卡文 4 張)、18326736 `main/①`(票60 的編號前綴標頭)。

**與[[官方明示]]一致而留 `official`**:0 條。

**三道關卡**:`關卡: 0 個問題`。`結果檔對不到任何一行` / `改判動不了的來源` / `序號引用對不到編號` 皆 0。未用 `--force`。

**`rule_llm_conflict`**:**3 筆,與票43–46 相同、本票沒有新增**(13857930、17732278、40227329,R17 看不到母句的已知盲點)。

**獨立驗證**:[[必發/選發]] 880/881 = **99.9%** PASS、[[效果類型]] 4834/4837 = **99.9%** PASS;17 條規則全部生效。

**判到的硬骨頭**(四張,都寫進規範了):
- 蠕蟲 零 74506079 的三個 `●`:官方寫 `いずれの『●』の効果も、…起動効果です`。票31 當時把它留空,但票38 已經定案**全稱句不是歧義** → 本票三段都填啟動效果,並回頭修了 §8 判準19 的舊記述。
- 扭力調整齒輪 79538761 ①:聯合怪獸「二選一發動」領起句,官方沒對它開口但對同構的 7 張明寫「効果として扱いません」→ 照家族判效果外文本(新寫進 §8 判準9)。
- 調弦的魔術師 73941492 `main/①`:官方開了 `【②の効果について】` 標頭但全卡只有 ①(§6 的「標頭編號官方寫錯」族)→ 照出現順序對回 ①,判誘發效果(1速)/選發。
- No.66 76067258 的兩個 `●`:官方寫 `『●』は永続効果です` 而卡上有兩個 `●`(§6 的真歧義)→ 兩段都是本卡在場上持續適用的段落,同判永續效果;第二個 `●` 的破壞替代代替手段是**場上**的另一張自分卡,§5.5 分界三不成立。

**規範更新**(`docs/effect_kind_guide.md`,三處):
1. §2.7 靈擺:票47 再 **26 張 / 30 條**,30/30、反例 0,全表現值 **361 條 / 303 張**。
2. §8 判準9 新增第三族**聯合怪獸的二選一發動領起句 → 效果外文本**,全表 8 條、官方 **7/7**、反例 0;並註明同句型寫在魔陷卡上時照 ADR-0004 填〈卡片種類〉卡效果(78765160 ②、24878656 ①)。
3. §8 判準19 修掉票31 的「三段留空」舊記述——票38 的全稱規則之後填得出來。

**規則層**:沒有新規則可登記,`rules.py` 未動,指紋 `7afec1581ae36cf0` 不變。

**測試**:6 個測試檔全跑,385 tests OK。

**剩餘池**:765 張 / 1,260 條 / 3 批(kind),外加票55 追蹤的 13 張 split。

**未收斂項**(沿用前票):衝突清單 3 筆不為空,規則層仍報 `→ 未収斂`。
