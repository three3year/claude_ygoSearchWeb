# 46 — 判定票 29/33 — 只判類型 kind-10

**What to build:** 只判類型系列第 10 批(300 張卡)的[[效果類型]]與[[必發/選發]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、45 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series kind --limit 1 --start 10` → `.scratch/tag-card/batches/kind-10.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。本系列的卡文已經有①②③編號,效果句集合已定,不需要拆句,§8 用不上。
- [x] **逐卡判定**:每個待判效果句的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/kind-10.json`(入版控,它是這一票的稽核軌跡)。判不出來的留空並寫 `note`,合併程序會把它們列進待審
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/kind-10.json --batch .scratch/tag-card/batches/kind-10.json --ticket 票46`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。本系列不動 §8
- [x] **回報**:本票判了幾條、與[[官方明示]]一致而留 `official` 幾條(這是準確率的免費量測)、留空待審幾條,以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 300 張。

## 票46 回報(2026-08-12)

**判定量**:300 張卡 / 300 段 / 488 條效果句。判定寫入 **483 條**,留空 **5 條**;結果檔給了[[必發/選發]] **103 條**,合併後全部落地且值一致(合併程序報「判定給的必發/選發 92 條」,差額 11 條是那些行上規則層/官方本來就已經有同值;`類型不承載必發/選發(已丟棄): 0 筆`)。

**留空 5 條**(與票40–45 同一批既有管線缺口,不是判不出來):
- 15415552 / 16237004 / 17350692 / 17782288 `main/1` — 無日文卡文的退化拆句(票55 追蹤的 4 張),照 §9 留空。
- 18326736 `main/①` — 官方逐項給了兩種類型但標頭帶編號前綴『①の』,抽取器不開火、管線沒拆,照 §9 末段留空(票60 追蹤)。

**與[[官方明示]]一致而留 `official`**:0 條。

**三道關卡**:`關卡: 0 個問題`。集合一致性自檢、拆句表三道驗證、判定對得回效果句全數 0;`結果檔對不到任何一行` / `改判動不了的來源` / `序號引用對不到編號` 皆 0。未用 `--force`。

**`rule_llm_conflict`**:**3 筆,與票43–45 相同、本票沒有新增**(13857930 `1-●2`、17732278 `①-●2`、40227329 `①-●1`,都是 R17 看不到母句的已知盲點,§5.10 末段已記)。

**獨立驗證**(合併程序內建,規則層遮答案跑官方明示):
- [[必發/選發]] 880/881 = **99.9%**(門檻 98%)PASS
- [[效果類型]] 4834/4837 = **99.9%**(門檻 99%)PASS
- 17 條規則逐條覆蓋 ≥ 8、一致率 100%(R4 99.7%、R7 99.7% 為已記錄的既有例外),全部生效。

**規範更新**(`docs/effect_kind_guide.md`,三處,全部量過全表):
1. §2.7 靈擺:票46 再 **37 張 / 42 條**,42/42 判靈擺魔法卡效果、反例 0,全表現值 **331 條 / 277 張**。
2. §5.8「被別張卡當成裝備魔法卡裝上去的怪獸,其 `●` 不跟母句」補上**全表量測**:`●` 判裝備魔法卡效果的行 26 條,扣掉母句是[[效果外文本]]的賦予型領起句(11 條)後,母句是**發動型**的 **7 張 / 13 條,13/13 全是裝備魔法卡效果、反例 0**(票46 貢獻裝甲超量 73046708 ①-●1②③)。73046708 與票42 的 39733924 是`①（②）の効果で装備魔法カード扱いとなったモンスターカードが…`這個句式的全表僅有 2 張。
3. §6「同一行裡的第二個類型詞在括弧內、又是『不會被無效化的效果』列舉時不算第二個答案」補上**全表量測**:這個括弧列舉全表恰好 **2 張**(票38 的 12067160、票46 的捕食植物 魔星花龍 69946549),**2/2 都照第一行的真明示填永續效果**。

**規則層**:本票沒有新規則可登記(覆蓋 ≥ 8 的新族沒有出現),`rules.py` 未動,指紋 `7afec1581ae36cf0` 不變。

**測試**:6 個測試檔全跑,385 tests OK。

**剩餘池**:1,060 張 / 1,746 條 / 4 批(kind),外加票55 追蹤的 13 張 split。

**未收斂項**(沿用前票,非本票造成):衝突清單 3 筆不為空,規則層因此仍報 `→ 未収斂`。
