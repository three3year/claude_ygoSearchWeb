# 28 — 判定票 11/33 — 要拆+判 split-12

**What to build:** 要拆+判系列第 12 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、27 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 12` → `.scratch/tag-card/batches/split-12.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-12.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-12.json --batch .scratch/tag-card/batches/split-12.json --ticket 票28`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票28 回報(2026-08-10)

- **拆句**:200 團寫入 193 團、拆成 **285 段**(效果句 234 + 效果外文本/交官方接手 51)。三道關卡 dry-run 修到 **0 問題**後正式寫入,未用 `--force`。
- **拆不出來 7 團**:28904860、42410161、43632709、53754391、54171327(以上五張與票27 同批)、54577949(古代的見證者)、55393975(具象天使 戰馬)——來源**沒有日文卡文**,照票23-27 同一處理(`clauses` 空 + note),票55 追蹤,下一票會原封不動再出現。
- **兩側語序不一致**:1 張給了 `ja_order`——幻蝶の刺客オオルリ 54582424(`[0, 2, 1]`,繁中把「シンクロ素材にできない」併進首行、日文在段尾,兩段效果外文本 index 0/0-2、效果句 index 1,官方明示モンスターの効果の扱いではありません)。
- **判定**:判了類型 **234 條**,其中寫入 llm **102 條**、與[[官方明示]]一致而由官方接手 **153 條**;[[必發/選發]]判定給值 **78 條**(必發 40 / 選發 38,合併程序採計 47 條、其餘由官方/規則先到)。**留空 0 條**(56784842 的 1 段依判準19 不自判、類型交官方標頭接手,不是判不出來)。
- **準確率(免費量測)**:因拆句在本票卡片上新取得[[官方明示]] **149 條**(source=official 行前 5 → 後 154),**與判定不一致 0 條**(類型 0、必發/選發 0;`遲到的官方明示與既有判定不一致` 0 筆、改判 0 條)。報告的 `既有判定對不到任何一行` 4 筆是管線先前在 55119278/57549932/57827484 的 `●` 上對位的 official 列(`1-●1` 等),本票依判準9 自行拆段後由新結構重新對位接手,值相同(永續效果×3/啟動效果),已驗證。
- **`rule_llm_conflict`**:本票 **0 筆**(衝突清單空)。規則層 13 條全部 PASS(整體獨立驗證 4435/4437 = 100.0%、必發/選發獨立驗證 865/866 = 99.9%),無規則異動,已收斂。R4/R7 各 1 條不一致(7540107、8903700)是既有線索,非本票引入。
- **歸納進規範**:§8 判準19 補**領起句自身是效果外文本的場合**——エンジェルＯ７ 56784842 的領起句(そのものは効果として扱いません)+單一 `●`、補足有 `【『●…』効果について】` 標頭,判定者拆成 0/1 兩段被管線再切成 `1-●1`、關卡三擋下(本票實測被打臉一次);照判準19 改寫**一段**不填類型、兩段類型由官方接手後過關。§8 判準13 補**獨立成句的「このカードを発動するターン、〜できない」發動回合限制**(未来王の予言 57274196,官方明示効果として扱いません→抽成效果外文本段),`check_split_rule.py` 掃描開火 7 段、引用被切開 0 段,覆蓋不足不併入掃描正則。**無新規則層規則**(無覆蓋 ≥ 8 的新族)。
- **值得記的個案**:聯合怪獸 ドイツ 57062206 照票56/split-10 的 47415292 樣板拆四段(裝備動作起動効果/裝備狀態攻守段/1体1枚限制效果外文本/破壞替代段);トイ・マジシャン 58132856 魔陷區覆蓋被破壞後的墓地觸發照票56 邊界判怪獸側誘發效果(官方明示誘発効果);ジャンク・コレクター 58242947 依獨立跨回合許可句(§5.6)判誘發即時效果(2速)選發;植物連鎖 54451023 陷阱裝備族照卡片種類判通常陷阱卡效果(官方無「装備魔法カードの効果として」句式,不改判);二重怪獸 2 張(57549932、57827484)照樣板拆「通常モンスター扱い」無種類效果+再度召喚領起句效果外文本+`●`。
- **測試**:tag_card 307(260+47)、card_list 29(16+13)、faq_info 43(14+29),全部通過。
