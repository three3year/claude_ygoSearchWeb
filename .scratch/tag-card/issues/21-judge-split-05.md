# 21 — 判定票 04/33 — 要拆+判 split-05

**What to build:** 要拆+判系列第 5 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、20 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 5` → `.scratch/tag-card/batches/split-05.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-05.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-05.json --batch .scratch/tag-card/batches/split-05.json --ticket 票21`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票21 回報(2026-08-10)

本票是 ADR-0004(魔陷卡效果九種)之後的第一張拆+判票,魔陷卡的卡發動/場上效果/持續段全部照 §5.8 新制填〈卡片種類〉卡效果。

- **拆句**:200 團全數寫入拆句表,拆成 **315 段**(效果句 277 + 效果外文本 38;單段 125 團 / 多段 75 團)。三道關卡 dry-run 第一輪 2 個問題(見下)、修正後 0 個,正式寫入未用 `--force`。
- **dry-run 打臉一次**:20349913(星河的施捨)繁中卡文尾端的譯名附註「※銀河的施捨」在管線的未拆整團裡已被剝除(全表 117 張),照批次檔頂層 `card_text_zh` 抄段落會過不了覆蓋檢查;`[按照判例修正]`(20436034)則保留、附在相鄰段落即可。已補記進 §8 硬性規則之後。
- **兩側語序不一致**:2 張給了 `ja_order`(破邪の刻印 17874674、アマリリース 18988391)——都是繁中把「維持代價」「1回合1次限制」提到最前面的形態,與票19/20 的樣板一致,`ja_order` 都是 `[1, 0]`。
- **判定**:判了類型 **274 條**,其中寫入 llm **116 條**(1 條與影子預測一致升 `llm_then_rule`:20663556 idx2)、與[[官方明示]]一致而由官方接手 **158 條**(官方在本票段落上共接手 174 列,含 16 列效果外文本);[[必發/選發]]判了 74 條、其中 **55 條**由判定寫入(其餘由官方明示階梯接手)。
- **留空 3 條**(理由在結果檔 note):17350692 與 17782288 整團留空(來源資料**缺日文卡文與補足**,同票20 的 15415552、16237004);22134079 idx3(`このカードの効果は「王家の眠る谷－ネクロバレー」の効果では無効化されない`,指稱全卡效果的免疫敘述、官方沒給類型,候選無種類效果 vs 永續效果——全表這族只有 1 條,不足以立判準)。
- **準確率(免費量測)**:因拆句在本票段落上新取得[[官方明示]] **174 條**,**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆)。
- **`rule_llm_conflict`**:本票新增 **0 筆**。批次帶[[影子預測]]的 7 條中,判成怪獸六類的全部一致;判成魔陷卡效果的 5 條(17078030、17178486、19844995、20065549、21770260)不在規則層管轄、照 ADR-0004 跳過比對不算衝突。**衝突清單現為空**——票20 記載的既有 1 筆(ヴァイロン・セグメント 1644289)已由票54 的 ADR-0004 整族改判解消,規則層收斂狀態轉為「**已收斂**」。
- **獨立驗證**:必發/選發 850/851(唯一不一致仍是 §6 記載的救世星龍 7841112);效果類型規則層 4295/4300,12 條規則全部 PASS 生效,無新規則進場(本票唯一的新族「効果は〜では無効化されない」全表 1 條,遠低於 8 條門檻)。
- **舊 ● 對位淘汰 5 列**:19357125(3 列)、20438745、21155323 先前由管線在未拆整團上拆出的 `●` official 列,在拆句後對不到新結構(報告的 `既有判定對不到任何一行` 5 筆),類型由官方在新段落上重新對位接手,值全部相同(永續×3、啟動×2 對應如舊),無資料損失。
- **歸納進規範**:§8 硬性規則後補「拆句對的原文是管線的未拆整團」一段(譯註剝除的來源說明)。無新拆句判準——本票拆點全部落在既有判準 5/9/12/13/16/17/20 與官方引用邊界上;帶 `●` 選項列舉的(20032555、21767650)照判準9 不拆、賦予型領起句的(19357125、20438745、20529766、21155323)照判準9 拆且領起句為效果外文本。
- **測試**:tag_card 294、faq_info 43、card_list 29,全部通過。
