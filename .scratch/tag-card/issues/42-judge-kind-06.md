# 42 — 判定票 25/33 — 只判類型 kind-06

**What to build:** 只判類型系列第 6 批(300 張卡)的[[效果類型]]與[[必發/選發]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、41 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series kind --limit 1 --start 6` → `.scratch/tag-card/batches/kind-06.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。本系列的卡文已經有①②③編號,效果句集合已定,不需要拆句,§8 用不上。
- [x] **逐卡判定**:每個待判效果句的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/kind-06.json`(入版控,它是這一票的稽核軌跡)。判不出來的留空並寫 `note`,合併程序會把它們列進待審
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/kind-06.json --batch .scratch/tag-card/batches/kind-06.json --ticket 票42`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。本系列不動 §8
- [x] **回報**:本票判了幾條、與[[官方明示]]一致而留 `official` 幾條(這是準確率的免費量測)、留空待審幾條,以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 300 張。

### 票42 回報(2026-08-12)

- **批次**:300 張卡 / **492 條**待判效果句(產生當下 `remaining_batches` 9)。合併後重跑 `make_batches --dry-run`:只判類型剩 **2,240 張 / 3,671 條 / 8 批**;要拆+判仍是 13 張(票55)。
- **判定**:判了 **487 條、留空 5 條**。三道關卡 dry-run **一次過 0 問題**,正式寫入未用 `--force`。
- **準確率(免費量測)**:**官方接手 0 條**(只判類型系列的批次是官方對不出或沒給的殘餘池);`遲到的官方明示升為 official` 0 條、不一致 0 筆。
- **`rule_llm_conflict`**:**3 筆**(票38/39 的兩條 R17 已知固定誤判 13857930、17732278,**本票新增第 3 筆** 40227329 `①-●1`)。新增這筆**不是判錯**:母句 ① 是官方明示的**啟動效果**、領起句是 §8 判準9 的列舉型(官方對整段只給一個類型、並寫明`処理時に、適用する『●』の効果を選びます`),而 `●このカードは直接攻撃できる` 命中 R17。R17 看不到母句,和前兩筆是同一種盲點;**不收緊 R17**(理由見規範 §5.10 末段)。規則層 17 條全部 PASS,必發/選發獨立驗證 880/881 = 99.9%、效果類型 4,834/4,837 = 99.9%。
- **類型分布**:魔陷側 **466 條**(通常陷阱 99 / 通常魔法 72 / 速攻魔法 63 / 場地魔法 60 / 永續陷阱 46 / 裝備魔法 40 / 永續魔法 35 / **靈擺魔法 29** / 反擊陷阱 13 / 儀式魔法 9),怪獸側 **21 條**(誘發效果 11 / 啟動效果 6 / 誘發即時效果 2 / 永續效果 1 / 無種類效果 1),留空 5。**96% 是魔陷卡效果**。
- **必發/選發**:結果檔給值 **81 條**(必發 14 / 選發 67),合併時 74 條真正由判定寫入(其餘 7 條官方/規則已有值),其餘照 §4 與 §5.8 留空。
- **留空那 5 條**:與票39/40/41 同一批卡、同一個理由(票55 的 4 張無日文卡文卡 + 票60 的 18326736)。
- **新規則(規則層)**:**無新增**,值域幾乎全在[[魔陷卡效果]]。
- **歸納進規範**(`docs/effect_kind_guide.md`):
  - **§5.8 補「装備魔法カード扱い」這個官方直說的身分明示**——三種措辭(`①の効果によって装備魔法カード扱いになっている状況で発動できる` / `装備魔法カード扱いの効果です` / `自身が装備魔法カード扱いになっている際に適用される効果です`),外加等價的`魔法カードの効果として扱います`。**全表量測**:補足含 `装備魔法カード扱い` 且印刷卡種是怪獸的卡 **118 張**、其上已判的裝備魔法卡效果 **113 條**;本票貢獻 **6 張 / 7 條**(37433748 ②、38601126 ②③、39299733 ②、39890958 ③、40028305 ②、40941889 ③),反例 0。
  - **§5.8 補「被別張卡當成裝備魔法卡的怪獸,其 `●` 不跟母句」**——全裝甲超量(39733924)② 是通常陷阱在墓地的發動,但兩個 `●` 官方寫`②の効果で装備魔法カード扱いとなったモンスターカードが…適用する`,講的是那張怪獸卡自己 → `●` 判裝備魔法卡效果、母句仍是通常陷阱卡效果。
  - **§2.4 / §5.10 的 R17 已知誤判**:由「固定 2 筆」更新為 **3 筆**,並寫明分界**不是**「母句是不是誘發效果」——40227329 的母句是啟動效果——而仍是「母句是發動型還是適用型」。
  - **§2.7 靈擺魔法卡效果**:本票 29 條 **29/29 判這個值、反例 0**(全表現值 **193 條**)。
- **值得記的個案**:
  - **光天使 杖(38331564)②**:`フィールドのこのカードを含むモンスター３体以上を素材としてX召喚したモンスターは以下の効果を得る` 正中 §5.5 分界四 → 領起句判**無種類效果**;被賦予的 `②-●1` 照 §5.5 末條自己判,官方寫「…X召喚したモンスターが効果を得て、そのX召喚に成功した時に効果を発動します」→ 誘發效果(1速)+選發。同一張卡上兩個值,是分界四最乾淨的示範。
  - **驚奇遊樂設施 驚險列車(36591747)②**:陷阱卡把自己放到裝備卡位置後才發動,官方寫`モンスターに装備されているこのカードが発動できる〜`——但照 §5.8「陷阱自我裝備族」仍判**通常陷阱卡效果**(有印刷卡種可以回去)。與上面那 6 張怪獸卡剛好成對照組。
  - **多多迷寶 軟綿雀精(42141493)**:母句 ① 沒被拆出來、官方只在 ① 上寫「手札で発動できる誘発即時効果です」,兩個 `●` 因此都判**誘發即時效果(2速)**;母句的發動子句是『自分・相手ターンに』(期間)+ 狀態條件 + 代價 → §5.8 第四/五列留空。
  - **`〜たターンのエンドフェイズに` 三筆**(39049051 ②、42149850 ②、42228966 ②):句子收在**時點詞**上,照票41 補的「期間的第三種寫法」反例側算固定階段的到來 → 填值。
