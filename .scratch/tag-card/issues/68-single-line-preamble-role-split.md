# 68 — 單行前言以句號＋role 拆(ADR-0012)

**What to build:** 前言拆分加第二層拆點:換行/引用合併分組之後,**組內**以「。」切句、以既有 role 正規式逐句判 role,相鄰句 role 相異處拆開、**同 role 相鄰句合為一段**;被引用括住(引用合併後跨句)的組不進第二層拆點,官方訊號照舊優先。完工後 34695290 的前言拆成兩列(召喚條件 + 使用次數限制,各自標效果外文本),全表約 100 張單行多 role 卡同步修正。

**Blocked by:** —(接續票 64–67 的前言拆分線)

**Status:** done

- [x] `_split_preamble` 第二層拆點:分組(`_quote_merge_groups` 之後)內逐句判 role,role 相異處設拆點;判 role 用中文單位側(與 `_preamble_role` 同一套正規式抽出逐句版本共用),中日句依既有 1:1 對位切段
- [x] 引用優先:組內拆點會切開引用時整組退回一段(不預先跳過——8192327 的引用括住的是同 role 相鄰句,合併後段界與引用天然一致);`_quotes_cut_apart` 底線驗證照舊釘在最終 spans 上
- [x] 測試:① 34695290 形態——單行兩句拆成 0-1 召喚條件 / 0-2 使用次數限制,官方明示(引用只括第一句)落到 0-1,兩列 kind 都是效果外文本;② 8192327 形態——單行三句「召喚、召喚、次數」,同 role 合併拆成 2 段;③ 儀式素材形態(藉由〜降臨。+ 次數句)拆成素材指定 + 使用次數限制;④ 引用括住整段(61901281 形態)維持 `"0"` 不拆;⑤ 跨句引用兩句同 role(通常召喚できない。〜特殊召喚できる 型)合為一段、引用不被切
- [x] 全表重跑對帳(對照票67 完工基準):拆分段數約 +100、role=使用次數限制約 +100 列、素材指定/召喚條件為拆出而非改判(總量不減)、退回清單不上升、`substring_violations` 0、`non_effect_outside_preamble` 不上升、既有判定孤兒 0
- [x] 遮蔽測試樣本 key 變動照 ADR-0009 前例(樣本過期),不另做遷移
- [x] 前端索引重建,以 `web/data.js` 核對 34695290(0-1 cond / 0-2 limit)與一張儀式形態卡
- [x] CONTEXT.md 效果句詞條邊界描述已更新(隨本票入 repo)

## Comments

2026-08-21 開票。背景與決定記在 ADR-0012,規格全文在 `spec-single-line-preamble-role-split.md`;三張機械規則救不了的卡(50604072、4168871、45286019)在票69。

2026-08-21 完工。全表重跑數字(對照票67 完工基準):

- 拆分 **1,483 段 → 1,577 段**(+94)、**3,111 列 → 3,327 列**(+216)
- role 分布:使用次數限制 5,499 → **5,605**(+106)、召喚條件 964 → **980**(+16)、素材指定 2,358 不變、null 845 不變——全部是拆出而非改判
- 退回清單 3 段不變(35606858 / 40080312 / 50604072,均為單位數不合)、`substring_violations` 0、`non_effect_outside_preamble` 84 不變、既有判定孤兒 0
- 待複查 4 筆:146746 / 33918636 / 82184400 / 83152482(連結怪獸,既有 `0-2` 的邊界被二次拆點改變——第二行拆成使用次數限制+連結素材限制,官方明示各自落段),逐筆人工核對結果正確後清旗標
- 引用優先的實作走「先拆再驗」而非「預先跳過」:16 張跨句引用全是同 role 相鄰句,合併後引用完整落在單一段內;role 相異而引用會被切開的(61901281 族)整組退回一段,實測全表零例外
- 殘餘 6 張未拆各有正當理由:11020863 / 12766474 / 23379054 / 63956833 日文是複合句(句中拆,拆句表領域)、22398665 中日結構不對等、18491580 官方引用括住整段(維持不拆是正確結果)——複合句族列入票69 的後續候選
- 前端索引重建全檢查通過,`web/data.js` 核對 34695290(cond / limit)與儀式卡 3909436(mat / limit)顯示資料正確
- 測試:TestPreambleSplit 新增 4 條場景測試,全檔 310+ 條全綠
