# 45 — 判定票 28/33 — 只判類型 kind-09

**What to build:** 只判類型系列第 9 批(300 張卡)的[[效果類型]]與[[必發/選發]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、44 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series kind --limit 1 --start 9` → `.scratch/tag-card/batches/kind-09.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。本系列的卡文已經有①②③編號,效果句集合已定,不需要拆句,§8 用不上。
- [x] **逐卡判定**:每個待判效果句的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/kind-09.json`(入版控,它是這一票的稽核軌跡)。判不出來的留空並寫 `note`,合併程序會把它們列進待審
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/kind-09.json --batch .scratch/tag-card/batches/kind-09.json --ticket 票45`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。本系列不動 §8
- [x] **回報**:本票判了幾條、與[[官方明示]]一致而留 `official` 幾條(這是準確率的免費量測)、留空待審幾條,以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 300 張。

## 票45 回報(2026-08-12)

**判定量**:300 張卡 / 300 段 / 519 條效果句。判定寫入 **514 條**,留空 **5 條**;結果檔給了[[必發/選發]] **110 條**,合併後全部落地且值一致(合併程序報「判定給的必發/選發 95 條」,差額 15 條是那些行上規則層/官方本來就已經有同值;`類型不承載必發/選發(已丟棄): 0 筆`)。

**留空 5 條**(與票40–44 同一批既有管線缺口,不是判不出來):
- 15415552 / 16237004 / 17350692 / 17782288 `main/1` — 無日文卡文的退化拆句(票55 追蹤的 4 張),照 §9 留空。
- 18326736 `main/①` — 官方逐項給了兩種類型但標頭帶編號前綴『①の』,抽取器不開火、管線沒拆,照 §9 末段留空(票60 追蹤)。

**與[[官方明示]]一致而留 `official`**:0 條。

**三道關卡**:`關卡: 0 個問題`。集合一致性自檢、拆句表三道驗證、判定對得回效果句全數 0;`結果檔對不到任何一行` / `改判動不了的來源` / `序號引用對不到編號` 皆 0。未用 `--force`。

**`rule_llm_conflict`**:**3 筆,與票43/票44 相同、本票沒有新增**(13857930 `1-●2`、17732278 `①-●2`、40227329 `①-●1`,都是 R17 看不到母句的已知盲點,§5.10 末段已記)。

**獨立驗證**(合併程序內建,規則層遮答案跑官方明示):
- [[必發/選發]] 880/881 = **99.9%**(門檻 98%)PASS
- [[效果類型]] 4834/4837 = **99.9%**(門檻 99%)PASS
- 17 條規則逐條覆蓋 ≥ 8、一致率 100%,全部生效。

**規範更新**(`docs/effect_kind_guide.md`,三處,全部量過全表):
1. §2.7 靈擺:票45 再 **19 張 / 21 條**,21/21 判靈擺魔法卡效果、反例 0,全表現值 **289 條**。
2. §5.8「永続魔法カード扱い」段落補上**陷阱側同族的全表量測**:`①：このカードが永続罠カード扱いの場合、…` 開頭的效果句 **10 條 / 9 張,10/10 全判永續陷阱卡效果、反例 0**(票45 貢獻 V.HERO 微光人 61320914 ②;官方在這一族同時給了身分與速度——`自身が永続罠カード扱いになっている際に発動する事ができる効果です` + `罠カードの効果として発動するスペルスピード２の効果として扱われます`)。判準與裝備側相同:怪獸卡以某個魔陷身分在魔陷區運作時,那一段就照該身分填〈卡片種類〉卡效果。
3. §5.8「装備されているこのカードが〜」族補一張兩段到齊的例子:偉魔龍神 絕火之瓦爾弗蘭 61272280 ②(持續段)③(發動段),兩段都判裝備魔法卡效果,③ 另照第一列填選發。

**測試**:6 個測試檔全跑,385 tests OK。

**剩餘池**:1,355 張 / 2,229 條 / 5 批(kind),外加票55 追蹤的 13 張 split。

**未收斂項**(沿用前票,非本票造成):衝突清單 3 筆不為空,規則層因此仍報 `→ 未収斂`。
