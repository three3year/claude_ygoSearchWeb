# 54 — 改判:魔陷效果句整族換魔陷卡效果值(ADR-0004)

**What to build:** 票53 稽核後,使用者定案:以魔法/陷阱卡身分運作的效果句不適用六類[[效果類型]],改判為以[[卡片種類]]命名的九個[[魔陷卡效果]]值(ADR-0004)。本票把既有 375 條(`llm` 365 + `llm_then_rule` 10)整族換值,走 `make_batches.py --rejudge` → 結果檔 → `merge_judgments.py` 的正式改判路徑。

**Blocked by:** 無(ADR-0004、guide 改寫、管線新值支援已在同一批 commit)

**Status:** done

## 換值規則(機械推導,無逐條人工判)

- `kind`:該卡[[卡片種類]] + 「卡效果」(例:通常陷阱 → 通常陷阱卡效果)。375 條全部是魔陷卡,無「怪獸當作魔陷卡」的行(普莉梅拉②③那種目前 kind=null,將來照新規範判)。
- `optional`:**有觸發事件的發動句保留**——現值為誘發效果(1速)且帶必發/選發的 55 條(必發 42 / 選發 13)原值照抄;其餘一律 null。2速選發的 150 條全數清掉:147 條是卡本身發動(玩家的選擇,無資訊量),雷瓶 11741041-2 是玩家主動起動、猛吹雪 473469 與偽陷阱 3027001 是卡本身發動(票53 分桶的誤傷,逐條看過)。
- `official` 60 條與效果外文本不動;改判本來也碰不到 official。

## 要做的事

- [x] `make_batches.py --rejudge <375 條> --name fix-54` 產改判批次
- [x] 機械產出結果檔(逐行 `rejudge: true` + 新值)
- [x] `merge_judgments.py --ticket 票54` 合併,關卡 0 問題
- [x] 複驗:重跑票53 稽核比對(官方明示零漂移、衝突 0)+ 全表重建冪等
- [x] 只 commit 到本地分支,不 push

## 結果

```
結果檔 301 段落:拆句 0 筆、拆不出來 0 筆、判定 375 條寫入、官方接手 0 條、改判 375 條
關卡: 0 個問題
```

新值分布:通常陷阱 107 / 永續陷阱 65 / 裝備魔法 60 / 通常魔法 49 / 永續魔法 32 / 速攻魔法 28 / 反擊陷阱 16 / 場地魔法 14 / 儀式魔法 4(合計 375)。optional:必發 42 / 選發 13 / null 320。

## 複驗(官方 QA 五道)

1. **稽核重跑**(票53 同一支比對):`official` 255 條全數與補足情報一致、0 mismatch;效果外文本反向敘述 0 條;375 條新值行官方全部沉默(確認 0/衝突 0/無證據 375)。
2. **反向漂移**:對全部魔陷卡重算 `attest()`,重算得出而表上不同的:**0 條**——官方明示一條不多、一條不少。
3. **改判冪等**:同一批次再跑 dry-run,改判 0 條、關卡 0 問題。
4. **全表重建冪等**:重建輸出與 `data/tag_cards.json` 逐位元組一致;**規則層衝突清單歸零**(改判前殘留的 1 筆正是魔陷行,ADR-0004 後退出規則層管轄)。
5. **單元測試**:294/294 通過。

## Comments

同批變更:ADR-0004、CONTEXT.md([[效果類型]]值域 15、新增[[魔陷卡效果]]詞條、[[必發/選發]]適用範圍、[[卡片種類]]引用)、guide(§1 流程加運作身分閘門並重編步驟、§2.7 新增、§4/§5.2/§5.6/§5.8/§5.9 改寫)、管線(`official.SPELLTRAP_KINDS`、`_apply_optional` 魔陷分支、`_compare_prediction` 跳過魔陷行、`kind_counts` 值域、`optional_on_wrong_kind` 放行)。

規則層收斂條件裡的「衝突清單為空」自本票起成立。遮蔽測試不受影響:樣本只從官方明示的六類行抽,全在三種保留形態裡。
