# 06 卡文詞彙表全掃的核可改寫批次進站

Status: resolved

## 來源

text-glossary#03 首跑全掃的審閱頁,站主逐群裁完後於 2026-08-25 指示
「剩下的全部照著期望調整」——**49 群 367 筆全數核可**,其中改寫 171 筆、
維持原文 196 筆。改寫筆即本票的進站對象。

擬稿與逐筆理由見審閱頁(產生器 `scratchpad/gen_report_page.py` +
規則 `scratchpad/expected_all.py`),裁示原委見 text-glossary#03 Comments。

## 範圍

**159 筆勘誤 / 155 張卡**(171 筆逐句擬稿去重後為 157,再把兩張「同一段
文字在整卡出現多次」的拆成帶前後文的定位,故 159):

- `49959355` 齊唱殭屍:①②兩句同錯,拆成兩筆、各用圈號定位
- `78665705` 古之咒文:同一句內兩處「原本的攻擊力、守備力」,拆成兩筆、
  各用前後文定位

與既有 4 張勘誤卡(`2504891`/`49820233`/`84488827`/`94997874`)**無重疊**。

清單由 `scratchpad/export_errata.py` 產出(去重+逐筆驗證),輸出
`scratchpad/errata06.json`。驗證項:每筆 `from` 在該卡全文**恰好出現 1 次**
(勘誤是整卡子字串替換,不唯一會誤傷)、補寫型的 `to` 不得已存在
(冪等,見 #04)。目前 **0 問題**。

## 步驟

1. `errata06.json` 併入 `data/text_errata.json`(既有 6 筆保留在前)
2. 重建 `cards.json`——**七個來源參數一個都不能少**(只給 `--zh` 會靜默
   清空日英卡名與 MD 稀有度;#05 差點中招):
   `--zh cards.cdb --ja ja-JP.cdb --en en-US.cdb --md md.json
   --genesys genesys.json --banlist-ocg banlist-ocg.json
   --banlist-tcg banlist-tcg.json`
3. `sync_errata_texts.py` **全表重跑**(#04 修好後不必再走 `--errata` 定向檔)
4. `build_index.py` 全檢查
5. 六目錄 Python 測試 + node 測試

## 驗收

- [x] `data/text_errata.json` **164 筆**(6+158),`build_cards.py` 報
      「卡文勘誤: 套用 164 筆」
- [x] `cards.json` 逐欄 diff:**只有 `desc` 變動、恰好 155 張**,與預期卡集
      完全相同;日英卡名、MD 稀有度、禁限、Genesys 皆無變動(七參數地雷未中)
- [x] `sync_errata_texts.py` 全表重跑:第一次標記表 158 行、拆句 0 筆;
      第二、三次皆 0 改動(#04 的冪等修復在此實測生效)
- [x] `build_index.py` **全部檢查通過**(勘誤逆推 0 筆、勘誤/改寫互斥 0 筆)
- [x] 六目錄 676 測 + node 120 測全綠
- [x] 重跑全掃另存 `scan-report-02.md`:全庫 337 筆(凍結時 634)。本批 155 張
      殘餘 9 筆,經查全是**同卡的其他句**、不在本批射程:7 筆 ドローする 用
      禁譯「從牌組抽」(第三筆裁示後才長出的 260 筆存量之一)、1 筆
      は発動できない、1 筆 攻撃できない(兩者皆為裁示「維持原文」的射程外類)

## Comments

2026-08-25 完成。實作中被**兩道建置期防線**擋下、各修一次:

1. **同卡兩筆勘誤的 `from` 互相包含**(`55521751`:一筆「〜的卡。」一筆
   「〜的卡」)——依序套用時後者找不到原文,`build_cards.py` 吵鬧失敗。
   逐筆獨立驗證看不出來,已在 `export_errata.py` 補「同卡依序套用」的模擬
   驗證,並自動略過被包含的那筆(留較長者,才會連標點一起換掉)。
2. **套用後 `to` 不唯一導致原文逆推失敗**(`93683815`:該卡 ① 本來就寫對、
   ② 才漏「域」,套用後全文出現兩個「在我方的靈擺區域放置」)——
   `build_index.py` 的「卡文勘誤逆推不回原文」擋下。改用帶句號的前後文定位,
   並在 `export_errata.py` 補**反向唯一性**驗證(ADR-0011 的 `og` 需要它)。

兩次都是**吵鬧失敗**擋在寫檔前,資料未受損(`cards.json` 皆未被覆蓋)。

**尚未處理的存量**(不在本票射程,另行開票):ドローする 用禁譯「從牌組抽」
**260 筆**——那是 2026-08-25 第三筆裁示「抽(牌)已含從牌組之意」後才長出來的,
凍結報告(8/24)不含此類。這是目前全庫 337 筆裡最大的一塊。
