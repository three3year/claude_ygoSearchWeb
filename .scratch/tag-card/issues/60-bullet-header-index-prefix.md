# 60 — `【①の『●３つ』の効果について】` 這種帶編號前綴的 `●` 標頭要認得

**What to build:** `official._BULLET_HEADER_RE` 的標頭形狀多認一種:`【〈編號〉の『●…』の効果について】`。官方用它逐項給了兩種類型,管線卻因為認不得標頭而不拆 `●`,判定者只能整段留空。

**Blocked by:** —(可立即開始;原阻擋的票58 已於 2026-08-11 完成)

**Status:** done

## 為什麼

守星騎士 托勒密(18326736)① 的卡文是一個列舉型領起句 + 兩個 `●`:

```
①：このカードのX素材を以下の数だけ取り除き、その効果を発動できる。
●３つ：…X召喚扱いでEXデッキから特殊召喚する。
●７つ：自分メインフェイズに発動できる。次の相手ターンをスキップする。
```

官方逐項給了**兩種不同的類型**:

```
【①の『●３つ』の効果について】
■モンスターゾーンで発動できる誘発即時効果です。
【①の『●７つ』の効果について】
■モンスターゾーンで発動できる起動効果です。
```

兩條授權路徑都沒開火:

- `_BULLET_HEADER_RE` 只認 `〈Nつ目の〉(『●…』|●)〔のな〕?(効果|処理)?について`,標頭前面多的 `①の` 讓它整條不匹配(`_bullet_spec` 回 None)。
- `bullets_typed_apart`(票58)要求**主語是 `●` 的行**,而這裡的類型詞寫在標頭底下的行裡,行內看不到 `●`。

後果與票58 一模一樣:抽取器讀出兩個類型 → 拒判(進報告的「同一效果句被官方寫成兩種類型」),管線不拆 → 一條效果句只有一個 `kind` 欄位,填哪一個都會讓另一個從查詢層消失。票39 照 `docs/effect_kind_guide.md` §9 末段留空。

## 工作契約

- [x] **先量範圍**:全表 `【…】` 標頭裡含 `●` 但 `_bullet_spec` 回 None 的有幾個、分別長什麼樣(不只編號前綴這一種就一起收);再看放寬之後會不會拆到**已經判過**的段(票58 的初版就是在這裡踩雷)
- [x] **實作**:`_BULLET_HEADER_RE` 允許 `〈①-⑩|1-9〉の` 前綴(或改成更一般的前置修飾),`_bullet_spec` 回傳的 label/ordinal 對位邏輯不變
- [x] **測試**:帶編號前綴的標頭 → 拆;標頭底下沒有類型詞 → 照 §8 判準17 的票38 修正**不影響拆句**(拆句依據是標頭本身,類型另算);引用橫跨拆點 → 仍不拆
- [x] **補判**:`make_batches --series kind --only 18326736 --name fix-60` → 判拆出來的行 → `merge_judgments --ticket 票60`
- [x] **文件**:`docs/effect_kind_guide.md` §9 的票39 段落改寫成「已修」;ADR-0003 視實作範圍決定要不要補一節
- [x] 只 commit 到本地分支,不 push

## Comments

出自票39(只判類型 kind-03)。該票已把個案寫進 §9,結果檔那一行留空並記了理由,合併程序會把它列進待審。

### 票60 回報(2026-08-13)

- **先量範圍**:全表 `【…】` 標頭裡含 `●` 但 `_bullet_spec` 回 None 的共 **38 個**。其中 36 個不該認 —— 33 個是 `『…融合召喚及び以下の方法でのみ特殊召喚できる。●…』について` 這種**引用整段卡文**的標頭(走引號對位那條路)、3 個是 `『●』の方法による特殊召喚について` / `●の方法による特殊召喚について` / 截斷的 `●自身のLPが…`(都沒有類型詞)。真正該收的只有 18326736 的兩個。
- **實作**:`_BULLET_HEADER_RE` 前面多一個選用的 `〈編號〉の` 前綴,`_bullet_spec` 回傳的 label / ordinal 對位邏輯**不變** —— 前綴只說了在哪一個編號效果底下,而 `_resolve_bullet` 本來就要求全部的 `●` 出自同一個編號效果。放寬後多認得的標頭就是那 2 個,沒有拆到任何已經判過的段(`orphaned_judgments` 0 筆)。
- **測試** 3 條(`TestIndexPrefixedBulletHeader`,建置管線頂層縫隙):帶編號前綴的標頭會拆並逐項給類型、標頭底下沒有類型詞時仍然拆、引用橫跨拆點時仍不拆。
- **補判**:`make_batches --series kind --only 18326736 --name fix-60` → `①` 判**效果外文本** → `merge_judgments --ticket 票60`,三道關卡 0 問題。判定理由:領起句 `①：このカードのX素材を以下の数だけ取り除き、その効果を発動できる。` 只宣告代價與「可以開其中一個」,兩個 `●` 各自帶著自己的發動子句與時點、官方也各給了不同的類型(誘発即時効果 / 起動効果),領起句撐不起其中任何一個 —— 與票47 的聯合怪獸族(7/7 效果外文本)、票58 的 3657444 `①` 同一個形狀。
- **全表影響**:效果句 32,593 → **32,595**,官方明示 +2(兩個 `●` 當場由標頭對位接手),「同一效果句被官方寫成兩種類型」 **12 → 11 筆**,`kind: null` **18 → 17 條**(剩下的 17 條是票55 的 17 張)。
- **ADR-0003 不補節**:本票只是標頭正則多認一種前置修飾,拆句依據(官方以 `●` 分項描述子效果)與資料形態都沒有變。
- **文件**:`docs/effect_kind_guide.md` §9 的票39 段落改寫成「已修」,補上全表量測與拆完的結果。
