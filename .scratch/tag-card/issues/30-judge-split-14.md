# 30 — 判定票 13/33 — 要拆+判 split-14

**What to build:** 要拆+判系列第 14 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、29 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 14` → `.scratch/tag-card/batches/split-14.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-14.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-14.json --batch .scratch/tag-card/batches/split-14.json --ticket 票30`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票30 回報(2026-08-10)

- **拆句**:200 團寫入 192 團、拆成 **327 段**;管線再把 69831560 的 `●` 段切成三段後,本票 200 張在標記表上共 337 條(效果外文本 52 + 效果句 277 + 未拆整團 8)。三道關卡 dry-run **一次過 0 問題**,正式寫入未用 `--force`。
- **拆不出來 8 團**:28904860、42410161、43632709、53754391、54171327、54577949、55393975(票23-29 同一批)加上本票新出現的 68811206——來源**沒有日文卡文**,照既有處理(`clauses` 空 + note),票55 追蹤,下一票會原封不動再出現。
- **兩側語序不一致**:2 張給了 `ja_order`——オーロラ・ウィング 70089580(`[1, 0]`,卡名次數限制提前)、機械王－Ｂ．Ｃ．３０００ 70406920(`[1, 3, 0, 2]`,**本系列至今唯一的四段交錯**:繁中把發動回合限制提到最前、又把「罠カードとしても扱う」併在第二行,日文則是①②③④順讀)。`index` 全部照日文序號給(判準11)。
- **判定**:判了類型 **274 條**,其中寫入 llm **109 條**、與[[官方明示]]一致而由官方接手 **188 行**(含 53 行我沒填類型、由官方或位置規則定案的效果外文本),另 30 行 index 0 由位置規則覆蓋。**類型留空 0 條**、**必發/選發留空 0 條**(缺日文的 8 團整團未拆,不計);必發/選發給值 **93 條**,合併程序採計 65 條、其餘由官方/規則先到。
- **準確率(免費量測)**:因拆句在本票卡片上新取得[[官方明示]] **180 條**(source=official 行前 10 → 後 190),**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆、改判 0 條)。報告的 `既有判定對不到任何一行` 8 筆是管線先前在 65260293 / 66331855 / 66712593 / 68762510 / 70595331 五張的 `●` 上對位的 official 列(`1-●1`/`1-●2`),本票依判準9/19 自行拆段後由新結構重新對位接手,值全部相同(永續效果×3 / 誘發效果(1速)×2 / 啟動效果×3),已逐張核對。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空。規則層新增 R14 後 14 條全部 PASS(整體獨立驗證 4558/4560 = 100.0%、必發/選發獨立驗證 868/869 = 99.9%)。R4/R7 各 1 條不一致(7540107、8903700)是**既有線索**,非本票引入。
- **新規則(規則層)**:**R14 無種類效果** —— 效果句同一句裡把適用範圍寫到墓地(`フィールド・墓地` / `墓地・フィールド` / `墓地またはフィールド`)又做「として扱う」,排除「発動」與觸發事件。這是 §5.5 分界一的機器形式,全表命中 **98 條**、官方給過答案的 **87/87** 全是無種類效果、反例 0,覆蓋遠超 MIN_COVERAGE。本票的二重(デュアル)怪獸 3 張(68366996、68762510、70595331)就是這一族。
- **歸納進規範**:
  - §2.1 補**魔陷卡的進場限制句**:`このカードは「〈某卡名〉」の効果でしかフィールドに出す事ができない`(死のメッセージ「Ａ」67287533 等 4 張)與怪獸的召喚條件同族,判效果外文本;官方只寫「カードの発動を行う事ができない魔法カードです」,不給類型。
  - §5.5 分界一補 R14 的登記與數字,規則表補一列。
  - §5.8 把**陷阱卡自己變成裝備卡**那一族寫進本文(票28/29 只記在票報告裡):`発動後このカードは装備カードとなり…` 的卡沒有變成「装備魔法カード扱い」,官方也不寫票56 的「装備魔法カードの効果として」句式,只寫「装備カードとして装備しているモンスターに適用される効果」;全表 17 條 / 13 張 **17/17** 都判印刷卡種的〈卡片種類〉卡效果。本票的燃える闘志 68054593 照此判通常陷阱卡效果。
  - §8 判準13 補「**しか特殊召喚できない**」變體(波動竜フォノン・ドラゴン 67556500,官方明示 `効果の扱いではありません`):全表同句型 **54/54** 效果外文本(官方明示 41 條);`(?<=。)(?=自分は「[^」]+」を[^。]*しか特殊召喚できない。)` 的掃描在未拆整團上開火 1 段、**引用被切開 0 段**(票30 時點),覆蓋只有 1,不併入判準13 的掃描正則。
- **值得記的個案**:アルカナフォースＥＸ 69831560 的補足有 `【『●表』の効果について】` 標頭,照判準19 只寫一段、`●` 交給管線拆,三段類型全由官方接手;另外 6 張帶 `●` 的卡沒有標頭,照判準9 自己拆(拆完領起句段不含 `●`、`●` 段不含賦予型領起句,`_bullet_authorised` 兩條路都不成立,管線不會再切)。Ｄ・ステープラン 型的「一個 `●` 兩種類型」出現 1 張(デュアル・ソルジャー 68366996,照判準18 再往下拆成永續+誘發)。聯合怪獸 2 張(六武衆の御霊代 65685470、コイツ 69456283)照 59364406 樣板拆五段。機械王－Ｂ．Ｃ．３０００ 70406920 是陷阱怪獸族,照カース・オブ・スタチュー 3129635 / 影依の原核 49514333 的先例:卡發動與「罠カードとしても扱う」判永續陷阱卡效果、變成怪獸後的效果判啟動效果。
- **測試**:tag_card 307、card_list 29、faq_info 43,全部通過。
