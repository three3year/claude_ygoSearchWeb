# 24 — 判定票 07/33 — 要拆+判 split-08

**What to build:** 要拆+判系列第 8 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、23 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 8` → `.scratch/tag-card/batches/split-08.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-08.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-08.json --batch .scratch/tag-card/batches/split-08.json --ticket 票24`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票24 回報(2026-08-10)

- **拆句**:200 團寫入 199 團、拆成 **315 段**(效果句 265 + 效果外文本 50;單段 121 團 / 多段 78 團)。三道關卡 dry-run 一次過 0 問題,正式寫入未用 `--force`。
- **拆不出來 1 團**:28904860(具象天使 堡壘)——來源仍然**沒有日文卡文**,照票23 同一處理(`clauses` 空 + note),留待來源補齊日文後再拆,下一票會原封不動再出現。
- **兩側語序不一致**:2 張給了 `ja_order`——奇巧人偶-暗影感觸者 34620088(繁中把日文末句「超量素材除外」提到最前,`ja_order` `[1, 2, 0]`,該段官方明示「効果モンスターの効果の扱いではありません」判效果外文本 `index "0"`)與縮退回路 36995273(繁中把維持代價提到最前、日文在句尾,`ja_order` `[1, 0]`、維持代價段 `index "0"`,與票19/21/23 樣板一致)。
- **判定**:判了類型 **265 條**,其中寫入 llm **114 條**、與[[官方明示]]一致而由官方接手 **165 條**、1 條與[[影子預測]]一致升 `llm_then_rule`;[[必發/選發]]寫入 **101 條**(合併後本票段落上必發 49 / 選發 53)。**留空 1 條**(28904860,無日文,見上)。
- **準確率(免費量測)**:因拆句在本票段落上新取得[[官方明示]] **166 條**(合併後本票段落 source=official 166 條;報告的 `既有判定對不到任何一行` 3 筆是管線先前在 35781051 / 38468214 兩張 `●` 上對位的 official 列,自行拆段後由新結構重新對位接手,值全部相同),**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆、改判 0 條)。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空,規則層 13 條全部 PASS(整體獨立驗證 4377/4379 = 100.0%、必發/選發獨立驗證 856/857 = 99.9%),無規則異動,已收斂。
- **歸納進規範**:§8 判準13 補**正向寫法的次數限制**一例(`「フォトン・パイレーツ」の効果は１ターンに２度まで使用できる`,36834155,抽成效果外文本段)——`まで(?:使用|発動)できる` 變體掃描開火 2 段、引用被切開 0 段,覆蓋只有 2 不併入掃描正則。**無新規則進規則層**(無覆蓋 ≥ 8 的新族)。其餘拆點全部落在既有判準 5/9/11/12/13/14/16/17/19 與官方引用邊界上:有【●…について】標頭的只寫一段交管線(34568403、35514096,判準19)、無標頭的●自行拆(35781051、38468214,判準9/19 同 8396952 先例)、官方引用整段跨句的不拆(34834619 儀式降臨句+リリース句一段、35975813 骰子+戰鬥無效化三句一段,硬性規則3)、尾端卡名限制句抽出(33611061、33698022、35537251、36376145、36834155,判準13)、裝備條件抽出(35220244 等 6 張,判準16)、官方另標類型的 `この効果で〜` 句照引用邊界拆(34294855、37675138、37781520、38107923,判準17)。
- **值得記的個案**:37364101(克己挑戰,裝備魔法)的同名唯一性限制句照 ADR-0004 與票22 先例(23562407/24845628)判**裝備魔法卡效果**;37120512(闇之破神劍)日文為舊式感嘆句單段、繁中裝備條件句無日文對應,整團一段判裝備魔法卡效果。
- **測試**:tag_card 294、card_list 29、faq_info 43,全部通過。
