# 66 — 引用跨行合併

**What to build:** 票65 退回的大宗救回來:補足情報 `『…』` 引用橫跨拆點時,不再整段退回,而是**把被引用括住的那幾行合為一段**再拆——官方逐項裁定視為一體的單位不切開。完工後 70488851 拆成 2 句(前兩行合為召喚條件 + 使用次數限制一句),退回清單大幅縮短,清單裡剩下的只有單位數真的對不上的段落。

**Blocked by:** 65 — 純換行前言拆分

**Status:** done

- [x] 拆點與引用比對:引用經正規化後落在日文前言的哪個行區間,跨行的引用把該區間合為一段;合併後所有引用必須完整落在單一段內,否則仍整段退回(不猜測的底線不變)
- [x] 中文分組照票65 的演算法,以合併後的段為分組目標
- [x] 測試(同票65 接縫):① 70488851 形態——三行、引用括前兩行,拆成 2 段且 role 分別為召喚條件/使用次數限制;② 引用型效果外明示落到正確細段(source 升 official);③ 合併後仍有引用被切開的構造 → 整段退回
- [x] 全表重跑:退回清單筆數顯著下降(票65 基準對比)、「效果外明示不在前言段」(`non_effect_outside_preamble`)筆數不得上升、`substring_violations` 維持 0
- [x] 無補足情報的卡行為與票65 完全相同(純換行),回歸測試釘住

## Comments

2026-08-14 完工。全表重跑數字(對照票65 基準):

- 退回清單 **56 → 3 段**,剩下的全是單位數不合(35606858 / 40080312 / 50604072),引用跨界 0
- 票65 退回的 53 段引用跨界:**29 段**合併後拆開(拆分 1,454 → 1,483 段、3,046 → 3,111 列),**24 段**引用把整段括成一體——官方視為一句,維持 `"0"` 不拆,是正確結果而非退回
- 70488851 拆成 2 句:前兩行合為召喚條件(引用型效果外明示落上去,source official)+ 使用次數限制一句
- `non_effect_outside_preamble` 84 → 84 未上升、`substring_violations` 0、既有判定孤兒 0
- role 分布:召喚條件 944 → 964、使用次數限制 5,485 → 5,499
- 測試③的說明:合併對重疊區間取**聯集**、段只會變大,「合併後仍有引用被切開」在結構上到不了(每個跨行引用的出現位置合併後必在單一段內);`_quotes_cut_apart` 檢查保留為不猜測的底線,測試改釘「合併後中文單位數仍不合 → 整段退回」與「引用括住全段 → 維持現狀」兩個可達的退回/不拆路徑
- 前端索引已重建,全部檢查通過
