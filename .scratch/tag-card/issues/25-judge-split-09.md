# 25 — 判定票 08/33 — 要拆+判 split-09

**What to build:** 要拆+判系列第 9 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、24 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 9` → `.scratch/tag-card/batches/split-09.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-09.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-09.json --batch .scratch/tag-card/batches/split-09.json --ticket 票25`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票25 回報(2026-08-10)

- **拆句**:200 團寫入 197 團、拆成 **324 段**(效果句 265 + 效果外文本 59)。三道關卡 dry-run **一次過 0 問題**,正式寫入未用 `--force`。
- **拆不出來 3 團**:28904860(具象天使 堡壘)、42410161(具象天使 沙特蘭卡)、43632709(森之聖獸 龍面花牝鹿)——來源**沒有日文卡文**,照票23/24 同一處理(`clauses` 空 + note),票55 追蹤,下一票會原封不動再出現。
- **兩側語序不一致**:2 張給了 `ja_order`——光子蜥蜴 38973775 與雷電栗子 40817915(繁中把卡名次數限制/「不能作同步素材」句提到最前、日文在句尾,`ja_order` 都是 `[1, 0]`、框架句段 `index "0"`,與票19/21/23/24 樣板一致;40817915 那句官方明示効果の扱いではありません)。
- **判定**:判了類型 **265 條**,其中寫入 llm **78 條**、與[[官方明示]]一致而由官方接手 **187 條**;[[必發/選發]]寫入 **107 條**(必發 63 / 選發 44)。**留空 0 條**(缺日文的 3 團整團未拆,不計)。
- **準確率(免費量測)**:因拆句在本票段落上新取得[[官方明示]] **207 條**(合併後本票 324 段中 source=official 207 條,含 20 條效果外文本段;報告的 `既有判定對不到任何一行` 3 筆是管線先前在 41147577 / 41309158 / 43114901 三張 `●` 上對位的 official 列,自行拆段後由新結構重新對位接手,值全部相同、已逐張驗證),**與判定不一致 0 條**(類型 0、必發/選發 0;`遲到的官方明示與既有判定不一致` 0 筆、改判 0 條)。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空,規則層 13 條全部 PASS(整體獨立驗證 4392/4394 = 100.0%、必發/選發獨立驗證 859/860 = 99.9%),無規則異動,已收斂。
- **歸納進規範**:§8 判準13 補**與發動限制連寫成一句**的變體(`「魔導書庫クレッセン」は１ターンに１枚しか発動できず、このカードを発動するターン、…発動できない`,40230018,整句抽成效果外文本段)——`「〜」は１ターンに１枚しか発動できず、` 變體掃描開火 1 段、引用被切開 0 段,覆蓋不足不併入掃描正則。**無新規則進規則層**(無覆蓋 ≥ 8 的新族)。其餘拆點全部落在既有判準 5/9/11/12/13/14/16/17/19 與官方引用邊界上:●無標頭自行拆(40371092 判準9)、領起句官方寫効果の扱いではありません的照官方(41147577、41309158、43114901,分界四)、尾端卡名限制句抽出(39699564、40424929、42082363,判準13)、裝備條件抽出(39774685、39897277、40830387,判準16)、官方另標類型的 `この効果で〜`/`この方法で〜` 句照引用邊界拆(39987164、41098335、41482598、42155488、42685062、40240595,判準17)、同一句內官方分判兩類的在引用邊界拆開(40217358 リリースできず/素材にもできない,判準4+17)。
- **值得記的個案**:38679204(大日神械星狀體)與 40267580(腦袋怪)的「装備カード扱い」段照 ADR-0004 普莉梅拉先例判**裝備魔法卡效果**(官方寫装備魔法カード扱いの効果/チェーンブロックの作られる,不給類型詞);41160595(召集魔法)是無法卡發動的魔法卡,墓地被動觸發段判通常魔法卡效果+必發;41482598(惡夢的蜃氣樓)兩段魔＆罠ゾーン發動效果照卡種判永續魔法卡效果、必發/選發照 §4 填必發。
- **測試**:tag_card 294、card_list 29、faq_info 43,全部通過。
