# 05 — Master Duel 稀有度整合

**What to build:** 總表每張卡新增 `md_rarity` 欄位(N/R/SR/UR;未實裝 MD 的卡為空字串)。稀有度資料來自 masterduelmeta.com API(`konamiID` 即卡片密碼,直接對齊;主卡沒對到時嘗試異圖密碼)。一鍵更新殼把 MDM API 抓取(分頁、需 User-Agent)納入來源下載,存成本地 JSON 後交管線整合;報告輸出 MD 稀有度覆蓋率。

**Blocked by:** 02 — 日英卡名整合

**Status:** resolved

- [x] 總表卡片含 `md_rarity` 欄位,值以密碼從 MD 稀有度來源對齊(以 fixture 驗證)
- [x] 主卡密碼沒對到、但異圖密碼有對到時,採用異圖的稀有度(以 fixture 驗證)
- [x] 來源缺卡(未實裝 MD)時欄位為空字串,不影響收錄(以 fixture 驗證)
- [x] 不給 MD 來源時行為與現況相同(欄位空字串、報告無 MD 覆蓋率段)
- [x] 報告輸出 MD 稀有度覆蓋率
- [x] 一鍵更新殼下載 MDM 全量(分頁至空頁為止、帶 User-Agent)為本地 JSON,離線模式沿用既有檔
- [x] 自動化測試不實際連網
- [x] 對真實資料執行,記錄實際覆蓋率

## Comments

2026-08-07 來源驗證:MDM API 全量 14,863 筆(5 頁),有效密碼→稀有度 13,820 筆;
對總表覆蓋 13,791/14,207(97.1%),缺漏即未實裝 MD 的卡。分布 N 5301/R 4008/SR 2883/UR 1628。
API 需 User-Agent 標頭(裸 urllib 403)。

2026-08-07 實作完成:真實資料 MD 稀有度覆蓋率 13791/14207(97.1%),未實裝 416 筆;
冪等驗證通過。抽查:青眼白龍 UR、解碼語者 UR。
