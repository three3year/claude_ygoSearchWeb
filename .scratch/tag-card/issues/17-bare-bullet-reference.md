# 17 — 官方以 `『●』` 代稱子效果但沒說是哪一個

**What to build:** [[官方明示]]寫 `『●』` 而該編號效果有兩個以上 `●` 時,歸屬對位對不出唯一標的,那一行只進 `quote_ambiguous` 報告(票16 重跑後 7 筆)。但其中兩種寫法官方**自己說了範圍**,現在被一起丟掉:

```
■『●』の効果はいずれも、…モンスターゾーンで発動できる起動効果です。   ← いずれも = 兩個都是
■『②』の１つ目の『●』は永続効果です。                                ← 序數已經指名
■同一チェーン上で２つ以上の『●』の効果の発動条件を満たした場合、…
```

`【１つ目の●について】` 標頭的序數解析已經寫在 `official._BULLET_HEADER_RE` / `_resolve_bullet()` 裡,行內引用沒有用上同一套。

**實測基準(2026-08-09,票16 重跑後)**

| 卡 | 形狀 | 條數 |
|---|---|---|
| `32013448` | `『●』の効果はいずれも…起動効果です` | 1 |
| `95442074` | `『②』の１つ目 / ２つ目の『●』は永続効果です` | 2 |
| `7614732`、`72305034`、`76067258` | 純 `『●』`,官方確實沒說是哪一個 | 4 |

**Blocked by:** —(票16 已把 `●` 拆出來,這裡只差歸屬對位)

**Status:** done

## 裁決(2026-08-12)

做。撿回來的只有 3 條(2 張卡),但補的是[[官方明示]]歸屬階梯的一個洞——封版之後[[差值更新]]是這份資料長期維護的方式,新卡踩到同一種寫法還是會掉進 `quote_ambiguous`,而那時沒有人在逐票讀報告。3 條資料無關痛癢,階梯留個洞不行。

## 工作契約

- [x] 「いずれも」/「２つ以上の」→ 同一個類型套到該編號效果**全部**的 `●`,需有測試
- [x] 行內的「Ｎつ目の『●』」沿用 `_resolve_bullet()` 的序數解析,需有測試
- [x] 其餘純 `『●』` 維持 `quote_ambiguous`——官方沒說就是沒說,不猜;放寬不得誤套到這 4 條,需有測試
- [x] 重跑全表,報告新取得官方明示的條數與 `quote_ambiguous` 的變化(預期 +3 / −3)
- [x] 測試加在建置管線的頂層縫隙上,照票16 的做法
- [x] 只 commit 到本地分支,不 push

### 票17 回報(2026-08-13)

- **實作**:`official._bullet_reference(line)` 取代 `_bullet_subject`,除了引用本身還回傳官方寫下的**範圍**——`("ordinal", N)` / `("all", None)` / None。序數沿用 `_ORDINALS`(與 `【Ｎつ目の●について】` 標頭同一份表);全稱認 `いずれの|どちらの|どの|全ての|すべての|Ｎつ(以上)の` 貼在引用之前,以及 `いずれも|どちらも` 出現在同一句話的引用之後。`_bullet_group()` 從 `_resolve_bullet` 抽出來,標頭與行內引用共用「同一組 `●`」的判別。
- **範圍落空不退回原文比對**:指名的第 N 個不存在時進 `quote_ambiguous`,不拿另一個 `●` 頂替官方指名的那一個。
- **實測與票面基準不同**(票面是 2026-08-09,票58 之後 `●` 拆得更多):`quote_ambiguous` **12 筆 → 1 筆**,官方明示 **17,375 → 17,391 條(+16)**,分別是 32013448(いずれも,2)、31632536(どちらの,2)、74506079(いずれの,3)、7614732(２つの,2)、3657444(序數,3)、72305034(序數,2)、95442074(`『②』の`+序數,2)。歸屬階梯:標頭 +7、序號 +2、原文引用 +7。
- **16 條全部與既有判定一致**(`late_official_conflicts` 0 筆、`late_official_upgrades` 16),等於白撿一次判定準確率的驗證。
- **剩下的 1 筆是真歧義**:CX 熱血指揮官 熱血龍 76067258 `『●』は永続効果です。`,卡上兩個 `●`,官方確實沒說是哪一個 —— 照票面維持不猜。
- **測試** 7 條(`TestBulletReferenceScope`,建置管線頂層縫隙):全稱在引用之後 / 之前、序數、序號+序數、標頭+全稱、光禿禿的 `『●』` 仍歧義、序數超出範圍不頂替。
- **文件**:`docs/effect_kind_guide.md` §9 的「官方用全稱一次指名全部 `●`」那一條改寫成「票38 定判準、票17 讓抽取器也吃得到」,補上序數那一半與剩下的 1 筆個案。
- **自審後收緊一次**:範圍解析**只問光禿禿的 `『●』`**。引用帶了原文時原文比對本身就是更強的證據(全表 251 行 `●` 主語裡有 219 行帶原文),範圍詞不該跟它搶標的。全表結果不變(仍是 +16 / `quote_ambiguous` 1 筆),多一條測試釘住這個優先序。
