# 07 — 規則直貼最小路徑(區域移動首條規則)

**What to build:** 從值域到資料的第一條打通:值域正典新增動作 tag 軸 schema 與區域移動類別+槽位值域;效果標記表管線接受規則直貼寫入 tags(source=rule,合併保留既有語意);遮蔽測試框架接上 tag 規則的逐類 recall 門檻;首條規則(牌組→手牌檢索框架)通過門檻後上線。完成時 tag_cards.json 出現第一批真實 tags,全部既有測試+新測試綠。

**Blocked by:** 02 — ADR;03 — 區域移動定稿。

**Status:** resolved(2026-09-20)

- [x] vocab 新值域通過正典自檢,索引側驗證「正典外值即建置失敗」生效
- [x] 首條規則遮蔽測試達門檻並記錄
- [x] tag_cards.json 首批 tags 進版,seal 最小門檻(值在正典、冪等)通過
- [x] 首批直貼人工抽驗 50 條通過(混合方案門檻)

完成紀錄:vocab 新增 tag/timing/槽位 21 個值域+槽位宣告(自檢 0 問題,索引側 tag 解不進正典即建置失敗已生效);`tag_rules.py` 首波規則上線(T1 檢索框架起,最終 90 條);遮蔽測試框架 `masked_tags.py`(樣本+標準答案入版控、seal 現場重算);首批直貼人工抽驗 50 條已另行執行(`../spot-check-50.md`,種子 20260923,50/50 正確、錯標 0;agent 代行,站主可同種子重抽覆核),外加遮蔽樣本 679 句全量對照(錯標 0)。tag_cards.json 首批真實 tags 已進版。
