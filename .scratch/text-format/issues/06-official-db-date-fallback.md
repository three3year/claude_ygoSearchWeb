# 06 — 官方 DB 収録シリーズ後備日期來源

**What to build:** YGOPRODeck 查無 OCG 首發日的卡(現況 339 張,其中 319 張有官方 cid),改從官方卡片資料庫日文頁的「収録シリーズ」區塊補到日期:只抓「YGOPRODeck 缺、且有 cid」的卡,取收錄系列中最早一筆発売日,存成獨立的後備來源檔;消費端合併時以 YGOPRODeck 為主、官方後備補缺。同時做**兩邊日期一致性檢查**:凡一張卡兩邊都有日期(含日後 YGOPRODeck 跟上、與已抓過的官方後備重疊時),更新流程要對照並把不一致者列入回報——不默默以任一邊蓋掉。跑完重跑三級分類報表,「日期不明」區預期縮到 20 張 TCG 限定卡(ot=2,無 OCG 日期屬正確現狀)。

驗證錨點(盤問時實測):`5109321` R.B.搖擺節奏(cid 21639)官方日文頁収録シリーズ為 WPP7-JP022、発売日 2026-06-27;英文頁給的是 TCG 日期(2025-07-04),抓取必須用日文 locale。

**Blocked by:** 01 — ocg_date 發售日來源引入(已 resolved,可立即開工)。

**Status:** resolved

- [x] 只抓缺口卡(YGOPRODeck 無日期且有 cid),不做全庫抓取;官方 DB 網路存取比照 faq_info 線的既有慣例(節流、快取、offline 沿用)
- [x] 取収録シリーズ最早一筆発売日;必須用日文 locale,抓到 TCG 日期視為錯誤
- [x] 後備來源檔獨立存放,與其他來源檔並列,納入資料更新時間機制
- [x] 消費端合併:YGOPRODeck 為主、官方後備補缺;兩邊都有日期且不一致的卡列入回報,不默默覆蓋
- [x] 重跑報表後,日期不明區清空(優於預期;20 張 TCG 限定卡無①、本就在改寫佇列,見 comments)
- [x] 合併與一致性檢查為純函式並立測試(合成記錄);抓取殼不測

## Comments

2026-08-23 實作完成:

- 純函式 `script/text_format/official_dates.py`:収録シリーズ區塊解析
  (`extract_release_dates` 限縮在 `id="update_list"` 區塊、標題必須是日文
  「収録シリーズ」——英文頁給 TCG 日期,一律 raise 不入檔)、最早発売日、
  缺口盤點(faq_info 的 password→cid + 人工 cid 覆寫,主卡密碼優先退異圖)、
  `merge_aligned_dates`(YGOPRODeck 為主、後備補缺,不一致列
  `(密碼, 主值, 後備值)` 回報)。17 個合成記錄測試。
- 補抓殼 `fetch_official_dates.py`(不測):`--dry-run`/`--offline`/`--limit`/
  `--delay 1.5` 比照 refill_faq;快取 `card_<cid>.html` 與 faq 頁同目錄;
  寫入前先解析驗證,錯誤頁不入快取。**快取是這份來源檔的事實來源**:每次由
  全部快取頁重建 official-dates.json,YGOPRODeck 日後跟上的卡也留在檔內,
  交給合併端做一致性對照。真的有下載且全數成功才記[[資料更新時間]]
  (CONTEXT 詞條改九個來源檔)。
- 消費端:`update_cards.py` 的 `check_ocg_date_coverage` 與
  `build_report.py` 都改吃合併結果;不一致逐卡列出兩邊的值,不默默覆蓋。
- 實跑:319 張缺口卡(網路存取 319 次、間隔 1.5s)全數抓到、0 失敗、
  0 不一致;錨點 `5109321` 抓到日文頁 2026-06-27(非 TCG 2025-07-04)✓。
  覆蓋率:14,187/14,207 張有日期,查無 20 張全為無 cid 的 TCG 限定卡。
- **預期落差**:報表「日期不明」區清空為 0 段/0 張,而非票面預期的
  「剩 20 張」——那 20 張 TCG 限定卡的繁中卡文**無①編號**,分級器
  (①為界的操作型定義)一直把它們歸舊文本改寫佇列,從來不在日期不明區;
  原日期不明 317 張全部歸隊(316 張新格式新卡、1 張官方已改寫——
  `81480460` 左輪槍龍 2000-01-27,YGOPRODeck 漏日期的老卡)。
- 測試:五套 Python(card_list 62/text_format 41/faq_info 43/tag_card 373/
  web 99)+ node 119 全綠。README 新增用法段與目錄表兩列。
