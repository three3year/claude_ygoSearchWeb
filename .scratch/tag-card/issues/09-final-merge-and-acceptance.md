# 09 — 最終合併驗收

**What to build:** [[效果標記表]]定版。全部判定票跑完後,做一次完整性檢查與收斂驗證,確認這份資料可以被後續的網站階段當作查詢基礎使用。

**Blocked by:** 13 — 拆句試點票,以及票 18–50(33 張判定票:18–36 為「要拆+判」split-02…split-20,37–50 為「只判類型」kind-01…kind-14;編號由票13 於 2026-08-09 配發)

**Status:** ready-for-agent

- [ ] 全表無殘留的 `kind: null`(純通常怪獸的空 `clauses` 除外)
- [ ] 收斂條件三項全數成立:規則清單本輪無異動、每條規則覆蓋 ≥ 8 條、`rule_llm_conflict` 清單為空
- [ ] 若收斂條件未成立,報告指出是哪一項、缺口多少,不強行定版
- [ ] 全表跑一次完整性檢查:每個效果句的 `text_zh` / `text_ja` 都是對應卡文的連續子字串;`optional` 只出現在誘發即時與誘發兩類;`role` 只出現在效果外文本
- [ ] [[拆句表]]的三道驗證全表失敗筆數為 0,且舊式整團**只剩判定者明講拆不出來的那些**沒有拆句表紀錄。這一批要逐筆列出理由供使用者裁決,不能靜靜留著——票13 開了這條出口(結果檔給空 `clauses` + `note`),試點 200 張裡有 3 張走它:2 張繁中與日文語序不一致(逐段對位在結構上不成立)、1 張來源沒有日文卡文。原票面寫的是「3,805 條全部有紀錄」,那在這條出口開通之前才成立
- [ ] 定版報告涵蓋:總卡數、總效果句數、五種 `source` 各自筆數與佔比、六種類型的分布、`confidence: low` 清單(預期 41 張卡——無補足的 697 張中有 673 張是純通常怪獸,`clauses` 為空、無效果句可標記)、待複查清單、以及三份官方明示待審清單的最終狀態
- [ ] 報告列出「歸屬由判定決定」的條目數,供抽查
- [ ] `data/tag_cards.json` 格式檢查:一卡一物件、依卡片密碼升冪、`indent=2`、保留結尾換行
- [ ] 更新 `README.md` 的目錄與後續階段說明,加入效果標記表與 `script/tag_card/`
- [ ] 只 commit 到本地分支,不 push
