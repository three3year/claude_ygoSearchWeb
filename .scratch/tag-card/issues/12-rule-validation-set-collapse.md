# 12 — 規則 R2 的獨立驗證集在票11 之後只剩 1 條

**What to build:** 效果類型規則層的獨立驗證只看「一致率 ≥ 99%」,不看**分母有多大**。票11 把未拆整團的官方明示撤掉之後,`R2`(發動子句以「リバース：」起首 → 誘發效果)的官方明示對照從 **71 條掉到 1 條**,而它仍然印著 `100.0% [PASS]` 照常上工。1/1 的一致率不是證據。

發現經過:票11 全表重跑後比對 `docs/effect_kind_rules.md`,R2 的「官方明示條數」欄從 71 變成 1。原因是合理的——「リバース：」是舊式卡文的句型,它原本的 70 條對照全是未拆整團被階梯四、五錯誤套上的類型,那些「標準答案」本來就不該算數。問題在於**撤掉之後沒有任何機制注意到這條規則已經失去驗證基礎**。

同一輪還有幾條規則的分母縮水但仍充足:R6 265→255、R7 535→486、R8 68→64、R9 42→41、R1 2,318→2,317。只有 R2 掉到個位數。

**Blocked by:** —(可立即開始)

**Status:** ready-for-agent

## 裁決(2026-08-12)

第三條路**實際發生了**:判定票系列跑完後舊式卡文全部拆開,拆出來的段落重新走了一次[[官方明示]],R2 的官方明示條數從 1 條長回 **86 條、一致率 100%**。觸發本票的個案已經自己消解。

但**仍然要補下限**,理由是這個洞的危害恰恰在封版之後才開始:封版之後[[效果標記表]]靠[[差值更新]]維護,不會再有人像 33 張判定票那樣逐條看過全表。R2 這次之所以被發現,是因為當時有人正在逐票比對 `docs/effect_kind_rules.md`;封版之後那個人不在了。而且這次分母塌掉**不是因為規則變爛,是因為資料變乾淨了**(票11 撤掉錯誤套用的官方明示)——同一件事會隨著資料層的每一次正當演進再發生。

現況全表最小驗證分母是 R12 的 10 條,設下限 8 攔不到任何現有規則。買的是無人看管時仍會出聲的斷言。

## 工作契約

- [ ] 新增與 `MIN_COVERAGE` 平行的**官方明示分母下限**常數,量級取相同
- [ ] 分母不足的規則,`docs/effect_kind_rules.md` 獨立驗證表的判讀欄印「不足」而非 `PASS`
- [ ] 分母不足**不停用規則**——理由與一致率未達門檻時相同:停用會把問題藏起來
- [ ] 列進票09 的收斂條件第四項
- [ ] 測試:分母不足時判讀不是 `PASS`、分母充足時行為不變、收斂條件會因分母不足而不成立
- [ ] 統計基準(2026-08-12):17 條規則全數 `PASS`,最小覆蓋 R12 的 13 條、最小驗證分母 R12 與 R16 的 10 條
- [ ] 只 commit 到本地分支,不 push

## Comments

規則層的兩道門檻現在是不對稱的:覆蓋條數有 `MIN_COVERAGE = 8` 的下限、低於就不上工,一致率有 `AGREEMENT_THRESHOLD = 0.99` 的門檻、但**分母沒有下限**。這次是被票11 撞出來的,但同一個洞在規則層每一次演進時都在。
