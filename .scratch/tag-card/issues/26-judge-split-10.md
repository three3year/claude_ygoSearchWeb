# 26 — 判定票 09/33 — 要拆+判 split-10

**What to build:** 要拆+判系列第 10 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、25 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 10` → `.scratch/tag-card/batches/split-10.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-10.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-10.json --batch .scratch/tag-card/batches/split-10.json --ticket 票26`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票26 回報(2026-08-10)

- **拆句**:200 團寫入 197 團、拆成 **308 段**(效果句 264 + 效果外文本 44)。三道關卡 dry-run **一次過 0 問題**,正式寫入未用 `--force`。
- **拆不出來 3 團**:28904860(具象天使 堡壘)、42410161(具象天使 沙特蘭卡)、43632709(森之聖獸 龍面花牝鹿)——來源**沒有日文卡文**,照票23-25 同一處理(`clauses` 空 + note),票55 追蹤,下一票會原封不動再出現。
- **兩側語序不一致**:2 張給了 `ja_order`——和平的使者 44656491(維持代價,官方明示効果の扱いではありません)與不協和音 46480475(§5.9 壽命句,官方明示罠カードの効果の扱いではありません;主動態「墓地へ送る」變體官方同樣不當效果)。繁中都把框架句提到最前、日文在句尾,`ja_order` 都是 `[1, 0]`、框架句段 `index "0"`,與票19/21 樣板一致。
- **判定**:判了類型 **264 條**,其中寫入 llm **100 條**、與[[官方明示]]一致而由官方接手 **185 條**(含效果外文本段的官方明示;另 23 段 index 0 由位置規則覆蓋);[[必發/選發]]判定給值 **95 條**(必發 49 / 選發 46,合併程序採計 61 條、其餘由官方/規則先到)。**留空 0 條**(缺日文的 3 團整團未拆,不計)。
- **準確率(免費量測)**:因拆句在本票段落上新取得[[官方明示]] **185 條**,**與判定不一致 0 條**(類型 0、必發/選發 0;`遲到的官方明示與既有判定不一致` 0 筆、改判 0 條)。報告的 `既有判定對不到任何一行` 5 筆是管線先前在 45593005 / 45662855 / 47459126 / 48868994 四張 `●` 上對位的 official 列,自行拆段後由新結構重新對位接手,值全部相同、已逐張驗證(48868994 的兩個 `●` 段官方補足寫在同一行、抽取器未對位,llm 判起動効果與補足原文一致)。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空,規則層 13 條全部 PASS(整體獨立驗證 4410/4412 = 100.0%、必發/選發獨立驗證 863/864 = 99.9%),無規則異動,已收斂。R4/R7 各 1 條不一致(7540107、8903700)是**既有線索**(票25 前即在),非本票引入,留待後續收緊。
- **歸納進規範**:§4 第一層補**選發側的官方明示**——補足寫「任意に発動する事ができる効果です」時判選發、壓過句尾規則。實測 2 條都是句尾規則會誤推必發的形態:海洋衛士 45045866 ②(句尾「加える」)、日珥龍 45985838 ②(句尾「与える」)。全表補足出現「任意に発動」816 處(歸屬未逐條核對),只記優先順序、不進抽取器。**無新拆句判準、無新規則層規則**(無覆蓋 ≥ 8 的新族)。
- **值得記的個案**:聯合怪獸 3 張(47228077、47415292、47693640)照票56 慣例拆判——裝備/解除複合句保留發動側判啟動效果(kind_conflicts 同族)、裝備狀態段(含括號破壞替代)判裝備魔法卡效果;光與闇之龍 47297616 ③ 是「觸發事件為效果的發動」族,官方明示誘発即時効果+必ず発動(§5.6 的 10 條必發 2 速族);二重怪獸 2 張(45662855、47459126)與表示形式 `●` 2 張(45593005、48868994)照判準 9/19 自行拆,官方全數接手;聖遺物 2 張(47863787、48086335)的自我覆蓋句照 12697630 先例判無種類效果。
- **測試**:tag_card 307(260+47)、card_list 29(16+13)、faq_info 43(29+14),全部通過。
