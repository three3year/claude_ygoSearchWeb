# 02 — 建置管線把資料更新時間寫進 META

**What to build:** 維護者重建[[前端索引]]後,META 帶著[[資料更新時間]]欄位
(值即票 01 記錄的時間);時間記錄不存在(clean checkout、從未跑過更新流程)
時欄位缺席、建置照常通過。`built_at` 照舊寫進 META 供維護者查。

**Blocked by:** 01 — 下載殼記下資料更新時間(欄位值與記錄格式由它定義)。

**Status:** done

- [x] 建置薄殼讀時間記錄,與 `built_at`、來源檔雜湊一樣以參數傳入建置管線純函式
- [x] 純函式不讀時鐘不讀檔;同一份輸入兩次建置逐位元組相同的性質不變
- [x] 有時間記錄 → META 出現資料更新時間欄位,序列化結果看得到
- [x] 無時間記錄 → META 無該欄位(不是空字串),不是建置失敗
- [x] 測試走既有建置管線純函式縫(prior art:webindex 測試)
