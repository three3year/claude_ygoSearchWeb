# 04 — 來源下載殼

**What to build:** 一個指令自動下載三個來源檔——salix5/cdb Releases 的 `cards.cdb`、mycard/ygopro-database 的 `ja-JP` 與 `en-US` cards.cdb——到不入版控的暫存目錄,接著執行管線產出總表;做到「一鍵從網路完成建置」。下載失敗時給出可讀錯誤並保留既有暫存檔,網路不通時仍可用已下載的來源檔重跑整合。

**Blocked by:** 02 — 日英卡名整合

**Status:** resolved

- [x] 單一指令完成:下載三來源 → 建置 → 產出 cards.json 與報告
- [x] 來源下載目錄不入版本控管
- [x] 處理本機已知的 TLS 憑證撤銷檢查問題(curl 需 `--ssl-no-revoke`,已實測;或改用 Python urllib 驗證可行)
- [x] 個別來源下載失敗時錯誤訊息指明是哪個來源,且不覆蓋/毀損既有暫存檔
- [x] 已有暫存來源檔時可跳過下載直接建置(離線重跑)
- [x] 自動化測試不實際連網(下載邏輯在管線接縫之外,測試僅涵蓋殼的參數與流程組裝)
