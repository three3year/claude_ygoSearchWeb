# 36 — 判定票 19/33 — 要拆+判 split-20

**What to build:** 要拆+判系列第 20 批(200 張卡)的[[效果句]]拆分與[[效果類型]]判定。本票完全自足:只讀 `docs/effect_kind_guide.md` 與自己的批次檔,不回頭查其他來源,也不需要前一票的記憶。

**Blocked by:** 06 — 影子預測與規則登記、07 — 遮蔽準確率測試、08 — 拆句表、判定結果合併與批次檔、13 — 拆句試點票、35 — 前一張判定票

**Status:** done

## 工作契約

- [x] **產生批次檔**:`python script/tag_card/make_batches.py --series split --limit 1 --start 20` → `.scratch/tag-card/batches/split-20.json`。批次即時產生,吃得到最新的規則層與最新的效果句集合
- [x] **讀規範**:`docs/effect_kind_guide.md` 從頭讀完。§8 是拆句判準所在,§5.5 / §5.6 / §5.8 是判類型最容易錯的地方。
- [x] **逐卡判定**:每一團拆成幾段、每段的繁中與日文原文**連續子字串**、每段的[[效果類型]]與[[必發/選發]]。判不出來的**留空並在 `note` 記理由**,不猜
- [x] **寫一個結果檔** `.scratch/tag-card/judgments/split-20.json`(入版控,它是這一票的稽核軌跡)。判定者不維護拆句表與效果標記表的一致性,那由合併程序保證
- [x] **合併**:`python script/tag_card/merge_judgments.py --result .scratch/tag-card/judgments/split-20.json --batch .scratch/tag-card/batches/split-20.json --ticket 票36`。先 `--dry-run` 跑一次三道關卡(集合一致性自檢、拆句表三道驗證、判定對得回效果句),問題修到 0 再正式寫入。**不要用 `--force`**
- [x] **歸納規則**:被打臉的地方寫進 `docs/effect_kind_guide.md`;可規則化的判定寫進 `script/tag_card/rules.py` 的影子預測層(覆蓋 ≥ 8 條才進)。**新增或修改 §8 的拆句判準前**,先用 `python script/tag_card/check_split_rule.py '<判準正則>'` 掃過全部未拆整團,確認它不會把官方 `『原文』` 引用切開,再寫進文件
- [x] **回報**:本票拆了幾段、幾團拆不出來(附理由)、判了幾條、因拆句新取得[[官方明示]]幾條、其中與判定不一致幾條(這是準確率的免費量測),以及 `rule_llm_conflict` 幾筆
- [x] 只 commit 到本地分支,不 push

## Comments

流程由票13(要拆+判的第一批 200 張)實測定案:三道關卡一次過、判定與回頭撿到的[[官方明示]]不一致 0 條。本系列每票 200 張。

### 票36 回報(2026-08-11)

- **本票是要拆+判系列的最後一批**,批次只有 **97 張**(不是 200):`make_batches --series split` 產生時 `remaining_batches` 已經是 1。合併後重跑 `make_batches --series split` 只剩 **13 張**,正是下面那 13 張沒有日文卡文的卡——**兩側都有卡文的整團全部拆完了**。
- **拆句**:97 團寫入 **84 團、拆成 115 段**;13 團整團未拆。本票 97 張在標記表上共 **128 條**(判定 115 + 缺日文的 13 團整團未拆)。三道關卡 dry-run **一次過 0 問題**(拆句 0 個問題、拆句表驗證一/二/三各 0 筆、結果檔對不到任何一行 0 筆),正式寫入未用 `--force`。
- **拆不出來 13 團**:28904860、42410161、43632709、53754391、54171327、54577949、55393975、68811206、80565021、80843006、81797573、81965543、89027418(與票35 同一批,一張不多一張不少)——來源**沒有日文卡文**,照既有處理(`clauses` 空 + note),票55 追蹤。
- **兩側語序不一致**:**1 張**。靈子能量固定裝置 99173029(`[1, 0, 2]`,繁中把**維持代價**提到最前,日文放在持續適用段與離場處理段中間),形狀與票32 的邁向破滅的速攻抽牌 77859858 一模一樣;`index` 照日文序號給,被提前的維持代價段是效果外文本、index `"0"`(判準11/5/15)。
- **判定**:判了類型 **115 條、留空 0 條**。合併後寫入 llm **46 條**、與[[官方明示]]一致而由官方接手 **62 條**、由位置/規則層接手 **7 條**(七段全是前言的效果外文本)。必發/選發給值 **40 條**(必發 16 / 選發 24)、其餘 75 條照 §4 與 §5.8 留空(卡本身的發動、不發動的段落、只寫主要階段的效果發動)。
- **準確率(免費量測)**:本票 97 張在合併前 **source=official 0 條**(97 團全部未拆、未判),拆完後 **62 條**——因拆句新取得[[官方明示]] **62 條**,**與判定不一致 0 條**(`遲到的官方明示與既有判定不一致` 0 筆、`既有判定對不到任何一行` 0 筆、`結果檔對不到任何一行` 0 筆);把位置/規則層接手的 7 條一起算,**69 條全部與判定相同**。
- **`rule_llm_conflict`**:本票 **0 筆**,衝突清單維持空。規則層新增 R17 後 17 條全部 PASS(整體獨立驗證 4833/4836 = 99.9%、必發/選發獨立驗證 880/882 = 99.8%)。R4/R7 各不一致 1 條(7540107、8903700)與 R7 的 77153811 都是**既有線索**,非本票引入。
- **新規則(規則層)**:**R17 永續效果** —— [[直接攻擊]]的許可或限制(`直接攻撃`),排除 `発動`、`リバース`、`召喚に成功したターン`、`手札から特殊召喚` 與觸發事件。全表命中 **160 條、官方給過答案的 134/134 全是永續效果**,反例 0。攻擊方式是怪獸在場上的持續狀態,無條件許可(402568)、附條件許可(3366982)、禁止(2896663)三種寫法官方都判永續效果。三個排除條件各有依據:`リバース` 是 §2.5 的觸發事件而規則層的 `_EVENT` 收不到它(5257687)、`召喚に成功したターン` 是 §5.5 分界五(32825095)、`手札から特殊召喚` 是 R6 自己的判別條件(18861006,少了它兩條規則會在同一句上結論不同而雙雙不寫)。
- **歸納進規範**:
  - §2.3 新增**直接攻擊整族**一節(R17 的登記、全表數字、三個排除條件各自的依據)。
  - §8 判準11 的實判率更新為 **3,791 團 / 45 團 = 1.2%**(票35 是 3,707 / 44),形狀分布重算(`[1, 0, 2]` 從 1 張長到 2 張);並補一條**「配不起來 ≠ 留空」**:整團本來就不必拆時不拆就解決了(積累的幸福 98444741 的發動條件被繁中併成句首條件子句),**先問該不該拆,再問配不配得起來**。
  - §8 判準16 補**「本判準吃的是日文卡文」**:覺醒 98374133 的日文整團沒有獨立的「〜のみ装備可能。」句,繁中卻多譯出一行裝備條件——照繁中拆會讓日文側切不出對應段落,兩側都保持 1 段。
  - §8 判準20 從 1 張長到 **3 張**(惡靈的魔導書 97997309、聖蛇的吹息 98850929),兩張都是判準13 掃描正則會直接命中的卡名發動限制句、卻夾在領起句與 `●` 之間;判別方式不是句型而是位置(**這一句後面還有沒有同一個效果的後半**)。
  - §8 票32 的列舉型領起句表補第四列:`〜以下の効果から１つ以上選んで発動できる`(可複選,全表只有 98850929 一張),官方寫`2つ以上の『●』の効果を選択して発動する場合、それぞれの『●』の効果の処理は同時に行われる扱いではありません`——多個 `●` 同屬一次卡發動,不拆。
  - §5.8 的必發/選發分界表第三列補**「只寫主要階段不必寫在卡文裡,官方在補足講的一樣算」**(遠古遺跡 代號A 99342953 ②:卡文是純代價、一個階段字都沒有,官方寫`自分のメインフェイズにのみ発動する事ができます`→留空),並釐清第四列指的是**官方也沒說**的那一種。
  - §5.8 的裝備動作 + 裝備狀態子效果族補手銬龍 97904474:官方第一個引用橫跨兩句,在句號處拆會切開引用,**兩句一段、照發動側判誘發效果(1速)**;它沒進 kind_conflicts 是因為`装備魔法カード扱いの効果となります`不是票56 登錄的身分句式。
- **拆句判準的安全性**:本票寫進 §8 的四處異動**全部是「要求少拆」的判準**(判準11 的不拆、判準16 的不拆、判準20 的兩張新實例、票32 表的第四列),不製造任何新拆點,照 §8 末段的規矩不必另外掃描論證。仍拿本票實際用到的兩條既有掃描正則重跑過:判準13(票28)的 `(?<=。)(?=このカードを発動するターン、自分は)` 與判準16 的 `(?<=装備可能。)(?=.)`,在**目前只剩 14 段的未拆整團池**上各開火 0 段、引用被切開 0 段。
- **值得記的個案**:靈擺欄本票 0 條。羽翼栗子球 LV10 98585345 是 §4 第三層那一族的第 7 條(`生け贄に捧げる事で` + 誘發即時 → 選發),但它由第二層的 `〈辭書形〉事で` 規則直接接手,不必動用第三層的具名清單。天使聖生 98273947 三段全是永續陷阱卡效果仍照樣拆成三段(§5.8:效果句是分群與貼 tag 的單位,不因類型相同而免拆)。迷宮的魔戰車 99551425 與骷髏機車手 99721536 是官方寫明「モンスター効果を持たない融合/儀式モンスターです」的整團效果外文本(index `"0"`),後者的繁中還少譯了日文的解放句,兩側各 1 段仍成立。
- **測試**:tag_card 307、card_list 29、faq_info 43,全部通過。
